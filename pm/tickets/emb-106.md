---
id: emb-106
title: v3 slice 6 — `nodes.json` optional (hostname default); stale dashboard files removed at boot
kind: normal
size: 1
status: dispatched
release: v3.0.0
updated: 2026-09-02
---

## Binding

**Why**: emb-99 charter. emb-89 made `nodes.json` mandatory to kill
the wrong `this-mac` default; for the single-machine daily case that
became pure friction (a file to hand-write before `embassy serve`
starts). The fix for a wrong default is a right default, not none.

**Deliverable** (base = emb-105 head):
- `loadGatewayNodeInventory`: on ENOENT of the state dir or the file,
  return an ATTESTED default inventory `{ host: os.hostname()
  short-name lower-cased, validated against HOST_TOKEN, fallback
  "localhost"; nodes: [] }` — it MUST go through `attest()` or the
  Claude/Codex providers refuse to construct. Present-but-unreadable
  still refuses (keep the emb-95 ENOENT/EPERM split); a present file
  is parsed exactly as today (multi-node unchanged).
- Delete `GATEWAY_NODE_INVENTORY_REQUIRED` and its CLI hint; fix
  `store.ts` claimable-empty (a state dir with only nodes.json) and
  the recovery copy ("keep nodes.json" → "nodes.json, if you use
  federation, is untouched").
- Boot cleanup (emb-100 F9): if `gateway-dashboard.html` /
  `gateway-dashboard.zh-CN.html` exist in the state dir (owned,
  mode 0600, regular files), unlink them once and record an activity;
  cutover runbook line.
- Tests: absent dir → default host = hostname; absent file → same;
  hostname failing HOST_TOKEN → "localhost"; unreadable file (EACCES)
  → still refuses with the classified code; present file → parsed
  unchanged; stale dashboard files removed at boot and never
  recreated. Docs: README quickstart step 1 loses the nodes.json
  prerequisite; CONFIGURATION.md nodes.json section becomes
  "optional; needed for federation"; CHANGELOG Unreleased (Changed).

## Record

- 2026-09-02 Drafted; dispatches after emb-105 lands.
- 2026-09-02 Dispatched on a PARALLEL lane (worktree v3-106, branch
  `v3/emb-106` off main 4d00fc4) to a Sonnet-tier engineer — Opus
  budget reserved for the in-flight emb-104; Fable limit until 15:30
  ET. Lands after emb-104 (PM rebases; expected conflicts: store.ts
  recovery copy, README quickstart, CHANGELOG Unreleased).
- 2026-09-02 FROZEN by the Sonnet engineer: head 7b1fdc3 on
  `v3/emb-106`, 12 files +156/−37; check 359/359 engineer and PM
  re-runs; scrubbed-HOME `health` no longer mentions nodes.json.
  Default = first hostname label lower-cased through HOST_TOKEN, else
  "localhost", attested via the same WeakSet path; ENOENT only; boot
  sweep lstat + uid + regular-file checks, best-effort unlink, one
  stderr line, symlink-safety test. Engineer note: the claimable-
  empty guard means the sweep only runs on marked (ours) directories.
  Adversarial read launched (Opus; Fable limit). Landing order: ahead
  of emb-104 (small, independent); 104 rebases.
- 2026-09-02 Adversarial read (Opus): HOLD. F1 README steps 2–4 still
  use `@this-mac` literally, so the quickstart's second command fails
  INVALID_ARGUMENTS with no hint (the deleted hint was the only text
  that ever named host identity); F2 identity from os.hostname() at
  every start is ambient and mutable — a DHCP/mDNS rename strands the
  ledger behind CORRUPT_GATEWAY_STATE with no converter (v3 removed
  1.9.3's rescue); F3 adopting federation later with a different
  `host` strands it the same way; F4 broker/CLI resolve identity
  independently (split-brain when the file appears mid-session); F5
  the wrong-state-dir diagnosis is gone; F6 the new doc guard cannot
  match markdown's backticked form; F7 doctrine sentence missing; F8
  chmod-000 test proves the mode check, not EACCES; F9 no end-to-end
  default boot; F10 sweep runs before the lock; F11 2.x temp files
  missed. Verified sound: attestation shape identical; ENOENT split
  exact; sweep cannot touch an unclaimed dir; TOCTOU not exploitable;
  stderr line cannot trip peer-client.
- PM ruling: the design flaw is the PM's (charter said "the fix for a
  wrong default is a right default" — a default must also be STABLE).
  Correction: the BROKER writes `nodes.json` on first boot from the
  hostname default (after lock + state validation), then reloads it,
  so identity always comes from the owned mode-0600 file and the CLI
  and broker read the same source; alias/host mismatch gets a named
  hint; README examples use `@HOST` with the ready-line sentence;
  socket hints name the state dir; guard regex fixed; sweep moved
  under the lock and extended to 2.x temp names; tests per F8/F9.
  Sent to the same engineer.
- 2026-09-02 Correction commit da5c27d (+462/−75; check 372/372
  engineer and PM re-runs): `ensureGatewayNodeInventoryFile` called
  at server.ts:167 right after `store.initialize({deferPersistence:
  true})` — exact bytes `{"version":1,"host":"<host>","nodes":[]}\n`,
  temp+rename, 0600, never rewrites a present file, reloads identity
  from the file, write failure refuses via the loader's classified
  codes; alias/host mismatch hint via a new `hintVars`/`renderHint`
  mechanism for register-codex (incl. --succeeds) and register-peer;
  README/SKILL/site examples use `@HOST` with the ready-line sentence;
  `controlSocketMissing` hint names the state dir; guard matches the
  backticked forms (verified against the base README); sweep moved
  after `loadStateFile()` and extended to the dotfile temp names;
  F8/F9 tests real-filesystem (realpath around /tmp). Second
  adversarial pass launched (Opus). NOTE for landing: emb-106 and
  emb-108 both rewrite the CONTROL_SOCKET_UNAVAILABLE hint — reconcile
  to one sentence at the emb-108 rebase (108's remedy text + 106's
  scrubbed-HOME clause).
- 2026-09-02 Second adversarial pass (Opus): HOLD. F1 the controller
  lock still gates stale-lock recovery on raw os.hostname() BEFORE
  the pid-liveness probe, so crash + network rename is a permanent
  retryable wedge — while this slice's docs promise a rename "never"
  matters; F2 `@HOST` is uppercase and rejected by ALIAS_PATTERN
  before the new hint can fire; site still says commands are exactly
  what the CLI accepts; SKILL has twelve with no legend; the docs
  test now pins the invalid form. Bundle: F3 written file never
  chmod'd (ACL/umask bricks the reload forever); F4 config/localHost/
  ready line still from the pre-lock read (comment overclaims); F5
  temp files orphaned on failure; F6 ENOSPC reported as invalid
  configuration; F7 win32 hint; F8 hint asserts a default as fact;
  F9 sweep before assertConfiguredBounds; F10 serve line omits the
  dir; F11 hint asymmetry; F12 E2E test leaks the lock; F13 guard
  misses "The file is mandatory"; F14 rename clobbers. Dispositions
  of the first pass: F8 F11 CLOSED, F1 REOPENED, rest PARTIAL.
- PM rulings: all fourteen to a NEW Opus engineer (tier rule): lock
  recovery by pid liveness with hostname informational (+ docs for
  the lock file); placeholder `@your-host` with a legend on every
  surface; chmod/unlink-on-failure/dir-fsync/link-no-replace;
  post-reload host equality assertion with a named code; honest
  write-failure classification; win32 arm untouched; default-vs-file
  hint forms; sweep after bounds; serve line with path; host check
  on unregister/await; non-leaking durability E2E with two boots;
  regex guard. Third cycle.
- 2026-09-02 Corrections 2 at dfd9efe by the Opus engineer (+517/
  −156; check 379/379 engineer and PM re-runs; `@HOST` zero): lock
  recovery by pid liveness (ESRCH → stale → rename-aside; live/EPERM
  → refuse; different recorded host + live pid → message names host,
  pid, and the remove-the-lock remedy; hostname bounded before
  quoting); lock file + GATEWAY_STATE_IN_USE documented; "never"
  sentences softened; `@your-host` with legends on four surfaces and
  a guard through the real alias grammar; writer chmod/link-no-
  clobber/dir-fsync/finally-unlink; GATEWAY_HOST_IDENTITY_CHANGED
  after the reload; GATEWAY_STATE_WRITE_FAILED classification; win32
  bare; file/defaulted hint forms via a DEFAULTED WeakSet; sweep
  after bounds (engineer notes loadStateFile already asserted
  bounds — cosmetic); serve line names the dir; host check on six
  alias arms after argument/identity validation; non-leaking two-boot
  durability E2E; regex guard verified on the base docs. Divergences
  declared: EEXIST lets the racing file stand (item 4 catches
  divergence); rename seam replaced by link; one extra CHANGELOG
  bullet for the lock change. Closure pass launched (Opus).
