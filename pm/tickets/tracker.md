---
project: Embassy
---

Tracker location config for the ticket-sync agent. The `project` above must
match the Linear project name; `team:` may be added if the workspace ever has
more than one team. This file is config, not a ticket.
