---
id: emb-111
title: v3.1 slice 1 — operating-contract truth: C1–C7 copy/docs/help repairs
kind: normal
size: 2
status: landed
release: unassigned
updated: 2026-09-05
---

## Binding

C1 the workspace-refusal hint claims state overlap for every CLAUDE_PEER_WORKSPACE_* reason while the real refusals are broad/unsafe/unattested (cli.ts ~190–191, ~661; test ~1774–1777 pins the false hint) → bounded, reason-specific guidance. C2 README jq .routes → jq .result.routes and document the {ok,command,result} envelope (ARCH ~262–264, skill ~43–49). C3 native advertisement prose: one per supervised helper, discoverable via ListAgents — delete the false exclusion (ARCH ~84–103; SECURITY ~219–224, ~279–280). C4 skill distribution: repo-shipped, operator-installed by copy (README ~78–83 vs skill:8 vs ARCH ~161). C5 ephemeral check: attributable rows and body removed, aggregate counters advance (README ~64; ARCH ~598–604); CHANGELOG 3.0.0 stays history. C6 body-output list includes await (ARCH ~619–622). C7 double-global-install note: one package manager; verify `which -a embassy` in the ssh launch environment. B5 --help lists --succeeds, --from/--to/--conversation, --expects-reply with exactly-one-target wording. Light gate (R1/R2). Proof per audit: documented selector run over byte-exact synthetic status output; help contains every accepted form; existing workspace acceptance tests kept.

## Record

- 2026-09-03 Priced from the emb-110 audit (artifact sha f1328dae…, base 8a5e7a1). Dispatched to swe3 as slice 1.
- 2026-09-03 CONTEST (pre-edit, budget): size 1 implies E1 ≤50
  changed lines, but the eight repairs map to cli.ts ≤35 (three
  reason-specific workspace hints + accepted help forms), focused
  tests ≤65, docs ≤100 across README/SECURITY/ARCHITECTURE/SKILL.
  GRANTED on the measured map: size 2 / E2 ≤200 changed lines, same
  scope, 0 new concepts, light gate. Engineer also flagged a crossed
  message: the quality addendum had already been delivered when my
  dispatch asked for it — it is priced (emb-118..125); no duplicate.
- 2026-09-03 FROZEN by swe3 (GPT-6 Astra): head ee616b1 on
  `v3.1/emb-111`, base 8a5e7a1 (PM verified ancestor), patch sha
  db3896fc… (PM verified), 9 files +78/−36 = 114/200 changed (CLI
  22/35, tests 32/65, docs 60/100), 0 new concepts; check 509/509
  engineer and PM re-runs; marker grep zero. Engineer's evidence:
  focused tests red on the parent then green (hint + help); README
  jq selector executed through the built CLI against a real private
  control server (`.result.routes` → []); ephemeral fixture proves
  no attributable rows/body while accepted/delivered/bytesAccepted
  advance; synthetic two-installation PATH for `which -a`; its own
  light adversarial read closed two items. Platform-bound tests:
  none added (existing darwin-only claude-peer test unchanged).
  Process note: the engineer's environment reviewer blocked the
  first branch push pending remote-ownership proof; a read-only
  GitHub metadata check satisfied it; no bypass. Adversarial read
  launched (Opus).
- 2026-09-03 Branch CI run 33983699786 on ee616b1: FAILURE on
  ubuntu/Node 20 only — one test, "queued peer mail resumes once after
  restart under the same hash-only principal" (real-token re-
  registration after restart answered `route_mismatch`); the other
  three legs green. emb-111's diff to that file is only the ephemeral
  aggregate pin; three local reruns at ee616b1 green (68/68). Failed
  job rerun: conclusion (READ) success — all five jobs green on
  ee616b1; the failure was a flake. Judged a pre-
  existing intermittent (the same test flaked on 7a7f3ce in the
  opposite direction, fixed by 4d00fc4 for the token case) — not
  attributed to the slice; swe3 told not to chase it here.
- 2026-09-03 Adversarial read (Opus): HOLD. F1 the rewritten
  advertisement sentences (SECURITY ~219–221, ARCH ~84–86/~94–95,
  SKILL ~20) still say every registry record is codex-* — the
  gateway publishes one marked record per NON-Claude route, codex-*
  or peer-* (proven by gateway-claude-helper.test.ts ~39–47 and two
  service tests); README:74 now contradicts SECURITY:219; F2
  CONTRIBUTING ~91–92 still says "one process-owned codex-* peer";
  F3 the SECURITY rewrite deleted the bound and stated a rate — the
  real cap is one helper/socket/record per non-Claude route up to
  maxHelpers = maxRoutes (128 default, 256 ceiling); F4 the install
  note omits the launchd plist, which pins an absolute program path
  (removing the pnpm copy after installing the service from it kills
  the broker while every listed check passes); F5 the new
  workspaceUnattested hint is unreachable via send (the code is
  converted to deferred/ROUTE_BUSY inside the provider) and pinned
  only by a synthesized fixture, while `status` shows the code bare;
  F6 two "nothing on disk" comments survive (test + store.ts R4); F7
  help truth not applied to unregister-codex / delivery-status /
  wait-delivery; F8 the "synthetic two-installation PATH test" is not
  in the frozen tests; F9 SKILL:8 lost the verb forbidding the agent
  itself to copy skills. Verified sound: both remaining workspace
  hints exact against their producers; prefix→exact narrowing loses
  no live code; help matches the parser byte-level; no collateral
  string changes; the README↔CLI jq coupling is a real oracle; C5/C6/
  C4 exact; test hygiene clean; scope confined to two string tables.
- PM rulings: nine-item correction to swe3 (budget E2 raised to ≤240
  for the doc lines; still light gate, 0 concepts): fix all four
  prefix sentences + CONTRIBUTING with a guard; restore the bound;
  plist path in the install note; remove the unreachable hint and
  add a STATUS_REMEDY instead; test comment now, store.ts comment to
  emb-122 (R4); sibling help lines; commit the PATH test or say ad
  hoc; restore the agent's no-copy verb.
- 2026-09-03 Pre-freeze contest on correction item (3): the engineer
  proved with a synthetic supervisor run that the helper bound is not
  enforced across pending creations (factoryCalls 2 with maxHelpers
  1). GRANTED: docs state the actual contract incl. "pending
  creations are not reserved"; runtime fix priced as emb-126.
  AGENTS.md:154–156 added to the docs window + guard. STATUS_REMEDY
  rendering consumer accepted. Progress 180/240 (+128/−52); check
  511/511; which-a proof to be labeled AD HOC.
- 2026-09-03 Engineer corrected the PM's granted wording: the displaced
  helper is NOT closed at supervisor shutdown (close() iterates only
  tracked helpers). Accepted verbatim; emb-126 widened to cover
  shutdown cleanup.
- 2026-09-03 FROZEN v2 by swe3: head d659ad1 (follow-up on ee616b1),
  base 8a5e7a1 (PM verified ancestor), full patch sha 237f1f63… and
  follow-up sha ebfebc85… (both PM verified), 14 files +130/−53 =
  183/240 changed (src 34/35, tests 63/65, docs 86/100), 0 new
  concepts; check 511/511 engineer and PM re-runs; marker grep zero;
  branch CI run 33985251226 success on the exact head (PM read).
  Engineer's closure: all advertisement guidance covers codex-* OR
  peer-* incl. AGENTS/CONTRIBUTING with a guard; SECURITY enumerates
  process/socket/record ownership, the tracked-count cap, and the
  accepted limitation verbatim; unreachable UNATTESTED CLI hint and
  its fixture deleted; STATUS_REMEDY + a targeted busy-route
  rendering consumer (table-only failed the new test red); sibling
  help lines; SKILL forbids the agent copying skills; README covers
  the plist's pinned absolute path, re-running service install, and
  service status's missing-path diagnosis; ephemeral test comment
  fixed; store.ts comment untouched (emb-122). Ad-hoc evidence
  labeled AD HOC (doc-proof sha 5a36b406…); emb-126 repro retained
  (sha 37ef2a6c…). Closure re-check launched (Opus) with a cross-file
  claim table as the deliverable.
- 2026-09-03 Closure re-check (Opus): HOLD. F1 SECURITY's new
  "ceiling 256" is FALSE — the number came from the PM's brief;
  providers.ts caps maxHelpers at 128 and throws above it, and the
  documented EMBASSY_MAX_ROUTES range 129–256 prevents broker start
  (→ emb-127); F2 "per non-Claude route" is false for federated
  mirrors, which fork a helper that always fails on every reconcile
  (→ emb-128); F3 the new STATUS_REMEDY route consumer is unscoped
  and preempts the stale/awaiting remedies; F4 the guard is
  paragraph-scoped and omits README; F5 CLAUDE_PEER_NOT_OBSERVED
  never renders on its row; F6 SKILL:231 "remove it on shutdown";
  F7 setup_help.yml singular label; SKILL:8 unpinned. Cross-file
  claim table delivered: 19 rows, three FALSE (all F1/F2). Verified
  sound: maxHelpers wiring, the limitation is the code, peer-* really
  published, UNATTESTED unreachability, README pins intact, help vs
  parser, oracles, discipline.
- PM rulings: seven-item follow-up to swe3 (+40 budget): docs state
  the cap as min(maxRoutes, 128) with no 256; "per LOCAL non-Claude
  route; federated mirrors are not advertised"; consumer scoped to
  busy/awaiting rows in the busy-observation family, no preemption;
  guard per-sentence incl. README; NOT_OBSERVED rendered by the same
  rule; SKILL:231 and setup_help wording; pin SKILL:8. Runtime fixes
  emb-127 and emb-128 filed, NOT in this slice. PM error recorded:
  the "ceiling 256" number was supplied by the PM without reading
  providers.ts.
- 2026-09-03 Bucket contest (pre-edit, follow-up 2): GRANTED —
  within the same ≤280 total, src ≤45 / tests ≤100 / docs ≤135
  (target ≤250); the scoped consumer + busy/stale/awaiting/disabled
  matrix + sentence-level guard do not fit the old sub-caps without
  weakening tests.
- 2026-09-05 Follow-up 2 FROZEN as emb-111 v3: head f53bde0 on
  origin/v3.1/emb-111, base 8a5e7a1 (= origin/main). PM verified in the
  gate worktree: head/base/full+follow-up patch SHA-256 all match the
  freeze; worktree clean; `TMPDIR=/tmp npm run check` 511 passed / 0
  failed; CI run 33986740929 READ at the exact head — macOS 20/22,
  Ubuntu 20/22, Inspect npm package all SUCCESS; accounting recomputed
  from the diff = src 36/45, tests 84/100, docs 100/135, total 220/280
  (matches the declaration); 0 conflict markers, diff --check clean,
  0 added TODO/FIXME. Docs now say "min(maxRoutes, 128)" and "federated
  mirrors are not advertised … reconciliation currently still attempts
  to create it" — honest about emb-127/128 being open. Opus closure
  re-check launched (items 1–7 + cross-file claim table).
- 2026-09-05 v3 closure re-check (Opus, narrow): HOLD. Items 1–7
  verified closed against the runtime (cap → providers.ts:363–367/:209;
  mirrors → claude-helper.ts:81; consumer precedence → status-view.ts
  :432 vs routeView; SKILL:8 pin; SKILL:231 vs service.ts:869–879;
  setup_help:26). Cross-file claim table: every shipped file agrees or
  is silent on cap / advertisement scope / displaced-helper caveat /
  ephemeral check / read-only status — except AGENTS.md:144–145, which
  still says "the one registered `codex-*` peer" (MAJOR), and the new
  forbidden-claim guard cannot see it because both rules require the
  literal noun "record" (MAJOR: phrase-independence not actually
  achieved; acceptance = replay over base 8a5e7a1 must catch the base
  CONTRIBUTING sentence and AGENTS:144–145, pass every legitimate
  sentence, both offenders pinned as inline negative fixtures). Three
  MINORs folded in: dead `awaiting` disjunct at status-view.ts:432;
  consumer covers 2 of 6 busy-observation codes (family = CLAUDE_CLEAN_
  PREWRITE_RETRY_CODES + NOT_OBSERVED + DISCOVERY_UNAVAILABLE) → one
  named set + four missing remedies + matrix case; UNATTESTED remedy
  says "report" instead of an operator move (trace what clears the
  missing state root). NIT: service test fixture's BROAD message keeps
  the false "contains the state directory" semantics. Sent to swe3
  (dlv_gqe32qA…); src cap → ≤55, total ≤280 unchanged. Freeze = v4.
- 2026-09-05 PM ERROR (second occurrence): after a `cd` into the
  records worktree the next reads ran there, so I "confirmed" doc lines
  from the v2-era records tree, not the frozen head, and briefly read a
  v3→v2 regression that did not exist. Caught before anything was sent.
  Norm: every Bash call in a multi-worktree session starts with an
  absolute `cd`; never rely on the persisted working directory.
- 2026-09-05 Follow-up 3 FROZEN as emb-111 v4: head 94573dc on
  origin/v3.1/emb-111, base 8a5e7a1 (= origin/main). PM verified:
  head/base/full+delta SHA-256 match; clean; check 511/511; CI run
  33988277580 READ at exact head — all five legs SUCCESS; accounting
  from the diff = src 53/55, tests 98/100, docs 104/135, total 255/280
  (matches); diff --check clean, 0 markers, 0 added TODO. Spot-check:
  AGENTS:144–145 now "any registered local `codex-*` or `peer-*` peer";
  guard predicate no longer keyed on "record" and pins both offender
  strings inline; CLAUDE_BUSY_OBSERVATION_CODES = the six producer codes
  with a call-site comment; four new remedies name refresh/status
  moves, UNATTESTED says "let the queued delivery retry" (engineer
  traced: discovery does not repopulate selectedStateRoots; routed
  preparation re-attests — claude-helper.ts:100–102, service.ts
  requeue ~2009–2022). Opus delta-only re-check launched (guard replay
  over base required, remedy trace per code).
- 2026-09-05 v4 closure re-check (Opus, delta-only): GO. Guard replay
  over base 8a5e7a1 rejected SEVEN sentences (both named offenders plus
  AGENTS:155, SECURITY:205, ARCH ×3) and nothing in the current tree;
  crafted singleton/legit sentences classified correctly; 6×6 remedy
  matrix = 84 assertions, 24 negative; UNATTESTED requeue→re-attest
  chain traced end to end (service.ts ~2011–2020 → ~1701–1707 →
  providers.ts ~571–587 → claude-helper.ts:101 → claude-peer.ts
  ~1113–1122) and pinned by test/claude-peer.test.ts ~458–469; BROAD
  fixture matches claude-peer.ts ~1075–1086. Residual MINORs/NITs
  (NOT_OBSERVED "retires on next send" false clause — pre-existing;
  DISCOVERY_UNAVAILABLE connector-code clause partial; TARGET_STALE/
  CHANGED have no producer; set not pinned to the provider set; guard
  quantifier vocabulary closed) → emb-129, size 1, dispatched next.
- 2026-09-05 LANDED: main fast-forwarded 8a5e7a1 → 94573dc (exact
  frozen head; no rebase, no squash). CI on main READ: run 33989042328 at 94573dc — macOS 20/22, Ubuntu
  20/22, Inspect npm package all SUCCESS.
  Four freeze cycles (ee616b1 → d659ad1 → f53bde0 → 94573dc); final
  255/280 changed, 0 new concepts.
