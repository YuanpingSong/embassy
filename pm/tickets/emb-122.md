---
id: emb-122
title: v3.1 quality 5 — name the durable rejection transaction (E7)
kind: normal
size: 1
status: queued
release: unassigned
updated: 2026-09-03
---

## Binding

store.ts ~1788–1796, ~1804–1815, ~1839–1847, ~1869–1885: one private rejectEnqueue(...): never performing recordRejection + CommitAndThrow; callers keep literal code/message/recoverable and ordering. Ordinary validation throws MUST NOT use it (they roll back). Net −15 to −25. FULL store gate (R4). Characterize first: a four-case matrix MESSAGE_TOO_LARGE / INVALID_DEADLINE / GATEWAY_RATE_LIMITED / GATEWAY_QUEUE_FULL with exact error tuples and deterministic persisted state; one rejection per call, no message, authority unchanged, restart readability, precedence validation→size→deadline→dedupe→rate→capacity; mutating each helper call away must break its own row.

**Rules for every quality slice** (from the emb-110 addendum, PM-
ratified): characterization tests are demonstrated green on the
UNCHANGED parent before the refactor, then land with it; no verbs,
flags, safe codes, wire versions, config, persisted fields, output
copy, authority decisions or replay behavior change; no B1–B4 fix
rides a quality slice; output bytes (stdout JSON key order, stderr
lines, exit codes, wire frames, envelopes, persisted state under
fixed clock/ids) stay identical; `TMPDIR=/tmp npm run check` green
and CI green before the next slice.

## Record

- 2026-09-03 Priced from the emb-110 quality addendum (sha cbaafc07…,
  base 8a5e7a1). Order: after emb-121.
- 2026-09-03 Rider from the emb-111 gate (R4 store comment, deferred
  here): store.ts ~947–950 still says an ephemeral registration's
  rows are "nothing … written afterwards either" — the truth is
  attributable rows/body removed, aggregate counters advance. Fix the
  comment in this slice (store.ts is already in scope); no behavior
  change.
