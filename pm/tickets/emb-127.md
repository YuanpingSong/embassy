---
id: emb-127
title: v3.1 correction — EMBASSY_MAX_ROUTES 129–256 prevents broker start (helper cap mismatch)
kind: bug
size: 1
status: dispatched
release: unassigned
updated: 2026-09-05
---

## Binding

docs/CONFIGURATION.md ~210 says EMBASSY_MAX_ROUTES accepts 2–256 and config.ts ~55 admits it, but server.ts ~182 passes maxRoutes as the Claude provider's maxHelpers and providers.ts ~363–367 `positiveBounded(value, 256 fallback, 128 maximum)` THROWS INVALID_GATEWAY_PROVIDER_CONFIGURATION above 128 — the broker never starts; under launchd (env captured into the plist) it crash-loops with a code the configuration doc says cannot occur (no test covers maxHelpers > 4). Deliverable: pick one truth and enforce it at config load — either cap maxHelpers at min(maxRoutes, 128) explicitly (documented), or reject EMBASSY_MAX_ROUTES > 128 at config time with INVALID_GATEWAY_CONFIGURATION naming the cause. No new concept. Characterize first (red on the parent): EMBASSY_MAX_ROUTES=200 → today's provider throw at start. R2: light gate + adversarial.

## Record

- 2026-09-03 Found by the emb-111 v2 closure pass (Opus); priced as a
  separate correction; ordered after emb-113 (dead seams) — small, independent.
- 2026-09-05 Gate v2 (founder-directed bundling): part of unit U1, position 3/4
  (pm/runbooks/gate-v2.md). One commit per ticket id on the unit lane;
  characterization shown green on the ticket's parent commit; landable prefix.
  U1 dispatched to swe3 from main d5be488.
