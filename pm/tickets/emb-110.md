---
id: emb-110
title: v3.1 quality pass — audit main against the shipped 3.0.0 contract; propose deletions AND behavior-preserving cleanups with evidence
kind: normal
size: 3
status: dispatched
release: unassigned
updated: 2026-09-03
---

## Binding

**Why**: founder, 2026-09-03, in session: "v3 has been successful in
achieving its stated goal but I think there is further room to make
our project even more efficient and focused." The founder updated
the swe3 seat to GPT-6 Astra (high reasoning) and asked for a
cleanup pass led by that seat. Same criteria as emb-99: daily value,
low maintenance, low operational friction.

**Founder broadening, 2026-09-03**: "the current pass is about current
quality — GPT-6 Astra has much better coding capability than GPT-5.6
Sol — use this new model to clean up any awkward code that we have. My
goal is actually a bit broader than just focus." So category (e)
below is first-class, not incidental.

**Deliverable (this ticket is AUDIT-FIRST; no edits)**: a read-only
audit of main at 8a5e7a1 against the shipped contract (README,
CHANGELOG 3.0.0, SECURITY, GATEWAY-ARCHITECTURE, the skill),
producing a candidate list the PM prices into deletion/correction
slices. Each candidate: what, file:lines, why it is dead/redundant/
drifted, deletion size, blast radius, and the regression evidence
that would prove the deletion safe. Categories: (a) dead code —
functions/exports/control params/safe codes never produced/hints
never rendered/config keys/test-only helpers shipped at runtime;
(b) redundant abstractions — e.g. the two Codex transports, the
Claude helper protocol layers, duplicated file readers, the store's
projection vs live shapes; (c) contract drift — any doc sentence not
true of the code; (d) the known backlog: destination-side handoff
refusal surfacing as `ambiguous` (should be a refusal code); no
token-free operator removal of stranded peer routes and no Claude-
route retirement; `snapshot.health` degraded-by-construction on an
idle broker; the envelope's reserve-time sourceAlias; `--help`
omitting --succeeds/--expects-reply/--from/--to/--conversation; the
m5dev pnpm+npm double install note. (e) CODE QUALITY — awkward or oversized functions, duplicated logic,
tangled control flow, unclear names, inconsistent error handling,
over-general helpers with one caller, comments that explain what the
code should have said: each candidate names the file:lines, the
better shape in a sentence, and the evidence that the refactor is
BEHAVIOR-PRESERVING (which tests already pin the behavior; which
wire/CLI outputs must stay byte-identical; what new test pins it if
none does). No new concepts (verbs, flags, codes, config, persisted
fields) ride a quality slice. Propose a slice order that keeps
`npm run check` green after each landing.

**Process**: same as v3 — each slice frozen with a handoff (base,
head, diff stat, concept accounting, platform-bound tests named),
PM check re-run + adversarial read, CI-green landing on main, records
per ticket. Quality refactors touching the send path, the store, or
the protocols get the FULL gate (adversarial + taste); elsewhere the
light gate. A refactor is priced like a deletion: value = code that
is easier to hold in the head, cost = risk × review time. No slice starts before the PM prices the candidate list.
The engineer's seat is swe3 (Codex CLI, GPT-6 Astra high); PM reviews
stay Opus/Fable.

## Record

- 2026-09-03 Dispatched to swe3 over Embassy 3.0.0 (conv_D4yH…).
- 2026-09-03 Founder broadened the pass to code quality (category
  e); addendum sent to swe3 on the same conversation.
- 2026-09-03 Linear mirror: 2185LAB-336 (In Progress, estimate 3,
  label normal). emb-99 mirror 2185LAB-325 moved to Done.
- 2026-09-03 AUDIT FROZEN by swe3 (GPT-6 Astra): artifact
  /tmp/emb-110-audit-report.md, sha f1328dae… (PM verified), base
  8a5e7a1, 0/0/0 edits, check 509/509 at base. Findings: A1–A5 ≈94–97
  removable src lines (unproduced ownedRoute path; unread
  observation-factory option; duplicate available-peer validators;
  test-only runtime facades; dead functions/import/barrel); B1 idle
  health inconsistent by construction (verified with a real service +
  injected clock: ok at t=0, degraded at t=35,001 from the local
  mailbox's age alone); B2 destination refusal lost across two
  boundaries, with the crucial caveat that receivePeerHandoff can
  throw AFTER commit so only a proven pre-enqueue refusal may become
  a distinct result; B3 token-free retirement genuinely absent (a new
  concept); B4 reserve-time sourceAlias reaches the envelope after a
  source rename; B5 help omits accepted flags; C1–C7 copy drift incl.
  a false workspace hint pinned by a test and the README jq selector
  addressing the wrong object; D1–D4 honest non-savings (both Codex
  transports, the Claude process layers, policy-distinct file
  readers, the projection) — no large deletion count claimed.
- PM PRICING: slices emb-111 (C1–C7+B5, light), emb-112 (A3/A4/A5,
  light + helper review; RULING: deep imports unsupported), emb-113
  (A1/A2, light + soak), emb-114 (B1, light + fake-clock), emb-115
  (B4, FULL), emb-116 (B2, FULL + drill), emb-117 (B3, FULL; NEW
  concept → founder prices; PM recommends yes). D1–D4 deferred as the
  audit advises; deadlinePressure/route.mutable removal is a public
  feature cut, not cleanup — not selected. GAP: category (e) code
  quality was requested after the audit began and is absent — a
  read-only supplement is requested alongside slice 1.
- 2026-09-03 Linear mirrors: emb-111→2185LAB-337 (In Progress),
  112→338, 113→339, 114→340, 115→341, 116→342, 117→343 (Backlog);
  emb-110 refreshed at 2185LAB-336.
- 2026-09-03 QUALITY ADDENDUM FROZEN by swe3: /tmp/emb-110-quality-
  addendum.md, sha cbaafc07… (PM verified), base 8a5e7a1, 0/0/0.
  Nine behavior-preserving candidates E1–E9 with existing pins and
  the missing before-refactor characterization named per item; a
  "do not refactor for tidiness" list (one-caller rollback
  transaction; linear discovery with rationale comments; the four
  deliberately different failure channels; sendToClaude/sendToCodex
  authority paths; D1–D4). Priced as emb-118 (E3+E1, FULL), emb-119
  (E4, FULL), emb-120 (E5, light), emb-121 (E6 client-only, FULL),
  emb-122 (E7, FULL store), emb-123 (E2, light + service suite),
  emb-124 (E8, FULL send path), emb-125 (E9, OPTIONAL — deferred).
  MASTER ORDER (interleaving the addendum's dependencies): emb-111 →
  emb-112 → emb-118 → emb-119 → emb-120 → emb-121 → emb-122 →
  emb-113 → emb-123 → emb-114 → emb-124 → emb-115 → emb-116 →
  (emb-125 if priced) → emb-117 (founder). Rule ratified: every
  quality slice lands characterization + refactor together after
  the characterization is shown green on the parent; no B-fix rides
  a quality slice; bytes identical.
- 2026-09-03 Linear mirrors for the quality slices: emb-118→2185LAB-
  344, 119→345, 120→346, 121→347, 122→348, 123→349, 124→350, 125→351
  (Backlog); emb-111 mirror re-estimated to 2.
- 2026-09-03 BACKLOG (characterization target for the quality pass,
  price with emb-122/125): test "queued peer mail resumes once after
  restart under the same hash-only principal" is intermittent on
  ubuntu/Node 20 — twice now: once accepting a wrong token (fixed,
  1-in-64 alphabet case) and once refusing the REAL token with
  route_mismatch after restart. The second is either a timing race
  in restart re-registration (mailbox restore vs re-register) or a
  test-ordering artifact; needs a deterministic characterization on
  the parent before any refactor near it.
