---
id: emb-105
title: v3 slice 5b — fold `reply` into `send --conversation`; keep `reply` as an alias for one release
kind: normal
size: 1
status: landed
release: v3.0.0
updated: 2026-09-02
---

## Binding

**Why**: emb-99 charter. With consent gone (emb-104) the only thing
`reply` did that `send` could not — answer from an unselected,
unpaired session via the transient-target path — no longer exists.
One verb fewer on the surface; threading is preserved because the
envelope is built from the conversation token, not from which RPC
was used.

**Deliverable** (base = emb-104 head): `send --conversation <token>`
as an alternative to `--to`, resolving target alias/binding from the
conversation record and keeping `assertReplyCaller` (caller owns one
end; binding unchanged else `CONVERSATION_ROUTE_RETIRED`); `reply`
stays as a thin alias (same handler) for one release because the
`<embassy-reply-hint>` text is in flight in existing envelopes — the
hint text itself switches to the `send --conversation` form. Tests:
a reply via `send --conversation` threads (same conversationId in
the envelope) and is refused for a non-owner; `reply` alias
byte-equivalent. Docs/skill/CHANGELOG Unreleased (Changed).

## Record

- 2026-09-02 Drafted; dispatches after emb-104 lands.
- 2026-09-02 Dispatched to an Opus engineer on worktree v3-105
  (branch `v3/emb-105` off main e1b2eed) from the pre-written brief.
- 2026-09-02 FROZEN by the Opus engineer: head 529d322 on
  `v3/emb-105` (base main e1b2eed), 14 files +318/−249 (src +109/
  −100); check 451/451 engineer and PM re-runs; marker grep zero.
  Design calls declared: the `reply` CONTROL METHOD is removed (both
  CLI verbs emit `send`; 14 methods now; protocol 3 unreleased, no
  bump); the conversation form forces expectsReply and REJECTS an
  explicit --expects-reply; one behavior change — a conversation send
  with no inherited identity reports CLAUDE_IDENTITY_REQUIRED (was
  CALLER_IDENTITY_REQUIRED), in CHANGELOG; `assertReplyCaller`'s
  unreachable arm dropped. Byte-identical wire test compares two real
  requests. Envelope hint teaches `embassy send --conversation
  <token> --from <alias>`. Flagged, not fixed: AGENTS.md:113 and
  CONTRIBUTING.md:208 still show `embassy reply … < body.md`
  (emb-109). Environment note: the pre-existing gateway-service suite
  fails under macOS's default TMPDIR (socket path length) — CI and
  the gate use TMPDIR=/tmp. Platform-bound tests: none. Adversarial
  read launched (Opus).
- 2026-09-02 Adversarial read (Opus): GO. The authority trace for
  the conversation path held under attempted inversion (per-principal
  proof identical or stronger than `--to`; all nine credential ×
  end-provider cross-claims closed; a leaked token alone never
  accepts; binding stability; no-replay flag kept; decoder closed;
  dropped arm unreachable). Findings, all prose/test strength: F1
  CHANGELOG Unreleased still describes a `reply` control result
  (lines 34/37) alongside the removal line (41); F2 SECURITY.md:178
  and DELIVERY.md:79 name only the deprecated spelling; F3 a peer or
  Codex caller following the hint without its credential now gets
  the Claude-specific CLAUDE_IDENTITY_REQUIRED (was provider-neutral
  CALLER_IDENTITY_REQUIRED); F4 CONTRIBUTING.md:208 (shipped) /
  AGENTS.md:113 / setup_help.yml:25 teach `embassy reply`; F5
  "reply" not in the retired-method UNKNOWN_METHOD list, and a stale
  client gets no remedy hint; F6 three claims held by code but not
  by the tests (deepEqual not bytes; direction inversion untested;
  no-credential shape confounded); F7 peer-alias grammar no longer
  enforced at the decoder for a peer-credentialed conversation send
  (fail-closed at the service; a widening the diff did not mention).
- PM ruling: land now (GO); F1–F7 go to emb-109 as pre-tag
  requirements — F1/F2/F4 docs, F3/F5/F6/F7 a small code rider
  (provider-neutral identity-required code; "reply" pinned retired +
  a rebuild hint on UNKNOWN_METHOD; byte-level wire assertion,
  direction-inversion test, unconfounded no-credential test;
  peer-alias grammar restored at the decoder).
- 2026-09-02 LANDED: main fast-forwarded e1b2eed → 529d322 (linear).
  CI run 33692641936 conclusion (READ): success on all five jobs.
  main is green at 529d322.
