---
id: emb-125
title: v3.1 quality 8 (OPTIONAL) — untangle the delivery runner (E9)
kind: normal
size: 3
status: draft
release: unassigned
updated: 2026-09-03
---

## Binding

service.ts ~1722–1976: keep runTarget as the reserve/revalidate coordinator; extract the two existing engines — peer attempt (~1784–1839) and provider attempt (~1841–1973) — returning the current retry decision; no generic engine, no new state enum. ~385–460 changed lines, net +10–25. FULL settlement gate (R4). Characterize first: an engine-by-phase throw table (prewrite / post-authorization / post-accept) with exact state, code, write count and zero later replay; frames and persisted/public rows byte-identical; keep even the coarse peer refusal during the refactor (B2/B4 are separate). PM: DEFERRED — price only after emb-122/124 show the smaller extractions paid off; the founder may weigh in.

**Rules for every quality slice** (from the emb-110 addendum, PM-
ratified): characterization tests are demonstrated green on the
UNCHANGED parent before the refactor, then land with it; no verbs,
flags, safe codes, wire versions, config, persisted fields, output
copy, authority decisions or replay behavior change; no B1–B4 fix
rides a quality slice; output bytes (stdout JSON key order, stderr
lines, exit codes, wire frames, envelopes, persisted state under
fixed clock/ids) stay identical; `TMPDIR=/tmp npm run check` green
and CI green before the next slice.

## Record

- 2026-09-03 Priced from the emb-110 quality addendum (sha cbaafc07…,
  base 8a5e7a1). Held; not dispatched.
