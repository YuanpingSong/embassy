---
id: emb-117
title: v3.1 slice 7 — operator retirement of a local route (B3) — NEW CONCEPT, founder-approved
kind: normal
size: 3
status: queued
release: unassigned
updated: 2026-09-05
---

## Binding

No token-free operator retirement exists for stranded durable peer routes (peer-v300/peer-v300b on this-mac) or exited Claude routes; the exact local removal/settlement primitive exists (store.ts ~960–995). Proposal: ONE explicit control + CLI verb, local routes only, no force mode, no guessed token, no remote mutation; settlement identical to unregister (queued/reserved cancelled, armed ambiguous, accepted unconfirmed); federated mirrors refused; conversations/native handles cleared. +40–80 lines, one new verb and one new control method. R4: FULL gate. PM recommendation: YES — it closes the stranded-route debt from the v3 drill and the emb-107 taste note; the concept is small and same-UID-scoped. Awaits the founder's pricing (new concept).

## Record

- 2026-09-03 Priced from the emb-110 audit (artifact sha f1328dae…, base 8a5e7a1). Drafted for founder pricing; not dispatched.
- 2026-09-05 FOUNDER: "emb-117 - yes." Priced as size 3 / E3 (≤500 changed;
  expected 80–150 with tests), FULL gate (R4: settlement + control
  protocol). PM sequencing: moved up to right after emb-112 — it removes
  user-visible clutter (peer-v300/peer-v300b on this-mac) and touches the
  removal primitive that emb-122 will later characterize, so it lands
  BEFORE the store refactor. Binding constraints restated for dispatch:
  local routes only; no force mode; no guessed token; federated mirrors
  refused with an exact code; settlement identical to unregister; ONE new
  verb + ONE new control method (this is the v3.1 line's single control-
  protocol bump — any other control change in the line rides the same
  bump); docs: README/ARCH/SKILL one sentence each + CHANGELOG Unreleased.
- 2026-09-05 Gate v2 (founder-directed bundling): part of unit U2, position 7/7
  (pm/runbooks/gate-v2.md). One commit per ticket id on the unit lane;
  characterization shown green on the ticket's parent commit; landable prefix.
- 2026-09-05 BOUND (pre-edit contest, U2): verb `embassy retire --alias
  <local-alias>` / control `retire_route` — matches the shipped "retire
  it" remedy copy; every local provider; federated mirrors refused with
  the existing FEDERATED_ROUTE_READ_ONLY; reuse atomic store removal +
  unregister settlement, no new activity vocabulary, no schema change;
  control v3→v4 (the line's only bump), peer wire v1, methods 14→15,
  CLI 17→18 with pins + current docs; remedies that say "retire it" now
  name the verb. Budget inside U2 ≈280.
