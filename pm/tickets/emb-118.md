---
id: emb-118
title: v3.1 quality 1 — one wire-failure reporter (E1) + rename the destination parser (E3)
kind: normal
size: 1
status: queued
release: unassigned
updated: 2026-09-03
---

## Binding

E1 cli.ts ~1291–1295, ~1319–1323, ~1350–1354, ~1454–1458 repeat writeFailure(kind=failure) + writeUnknownMethodHint + return; one private reportControlFailure(command, code, stdout, stderr) keeping each caller's branch; NOT for check-hop, peer-stdio, await-receipt or service failures (intentionally different). E3 rename the private requireClaudeSelector → requireDestinationSelector (it accepts any alias or Claude UUID); the exported predicate keeps its name. ~30–45 changed lines, net −4 to −8. FULL gate (the reporter sits on the send response path). Characterize first: all four call sites under UNKNOWN_METHOD and one other wire error incl. automatic check-target discovery failure, complete stdout/stderr bytes and exit; one success case per caller.

**Rules for every quality slice** (from the emb-110 addendum, PM-
ratified): characterization tests are demonstrated green on the
UNCHANGED parent before the refactor, then land with it; no verbs,
flags, safe codes, wire versions, config, persisted fields, output
copy, authority decisions or replay behavior change; no B1–B4 fix
rides a quality slice; output bytes (stdout JSON key order, stderr
lines, exit codes, wire frames, envelopes, persisted state under
fixed clock/ids) stay identical; `TMPDIR=/tmp npm run check` green
and CI green before the next slice.

## Record

- 2026-09-03 Priced from the emb-110 quality addendum (sha cbaafc07…,
  base 8a5e7a1). Order: after emb-111 (copy fixes precede goldens) and emb-112.
