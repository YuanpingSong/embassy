---
id: emb-136
title: Claude session resilience — duplicate session UUID handling and non-locking Claude retirement
status: active
opened: 2026-09-08
---
- 2026-09-08 INCIDENT (founder report): the infercat-pm Claude session restarted to load the Linear MCP; the old process stayed alive (pids 81444 ttys009 23:46 and 3148 ttys001 02:28, same sessionId 12b4a99c…). Embassy discovery → SESSION_ID_COLLISION → both bindings dropped → engineer's replies to infercat-pm@m5dev failed ROUTE_UNREGISTERED and the PM's own sends died. The PM then retired its own route (07:18Z) → nativeKey locked for retentionMs (24 h) and the alias refused → fully stuck; it asked the founder to "bind via embassy tui", which cannot bind Claude sessions (they are recorded on first send). Engineer's route was fine throughout (re-registered as reg_79fc…, idle, last DELIVERED).
- Fix now: (1) founder exits the duplicate claude process; (2) PM clears the retirement by running the broker briefly with EMBASSY_EVENT_TTL_MS=60000 then restoring defaults (no pending deliveries; only settled history ages out); (3) infercat-pm sends once → re-recorded. Both infercat-pm sessions messaged directly (Claude-native SendMessage) with the diagnosis and the wait instruction.
- Product unit dispatched to swe3 (4.6.0): A. duplicate UUID → bind newest live process, surface CLAUDE_SESSION_DUPLICATE with pids; B. Claude retirement non-locking for a reappearing live session (Codex successor semantics unchanged) + retire hint; C. docs/SKILL guidance. Tests for both scenarios.
- 2026-09-08 Founder stopped the duplicate process ("user error on my part"); verified: pid 81444 (ttys009) alone, one registry record for sessionId 12b4a99c…, no collision. PM cleared the retirement: `EMBASSY_EVENT_TTL_MS=60000 embassy service install` → loopback check to trigger prune → retirement gone → `embassy service install` restored defaults; broker healthy, 26 routes. Side effect: one accepted delivery to swe3 (the emb-136 unit message) was in flight during the reload → receipt likely unconfirmed; swe3 asked to confirm receipt. All-clear sent to infercat-pm [b2e301] with the one-send re-registration step.
