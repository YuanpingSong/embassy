---
id: emb-136
title: Claude session resilience — duplicate session UUID handling and non-locking Claude retirement
status: active
opened: 2026-09-08
---
- 2026-09-08 INCIDENT (founder report): the infercat-pm Claude session restarted to load the Linear MCP; the old process stayed alive (pids 81444 ttys009 23:46 and 3148 ttys001 02:28, same sessionId 12b4a99c…). Embassy discovery → SESSION_ID_COLLISION → both bindings dropped → engineer's replies to infercat-pm@m5dev failed ROUTE_UNREGISTERED and the PM's own sends died. The PM then retired its own route (07:18Z) → nativeKey locked for retentionMs (24 h) and the alias refused → fully stuck; it asked the founder to "bind via embassy tui", which cannot bind Claude sessions (they are recorded on first send). Engineer's route was fine throughout (re-registered as reg_79fc…, idle, last DELIVERED).
- Fix now: (1) founder exits the duplicate claude process; (2) PM clears the retirement by running the broker briefly with EMBASSY_EVENT_TTL_MS=60000 then restoring defaults (no pending deliveries; only settled history ages out); (3) infercat-pm sends once → re-recorded. Both infercat-pm sessions messaged directly (Claude-native SendMessage) with the diagnosis and the wait instruction.
- Product unit dispatched to swe3 (4.6.0): A. duplicate UUID → bind newest live process, surface CLAUDE_SESSION_DUPLICATE with pids; B. Claude retirement non-locking for a reappearing live session (Codex successor semantics unchanged) + retire hint; C. docs/SKILL guidance. Tests for both scenarios.
- 2026-09-08 Founder stopped the duplicate process ("user error on my part"); verified: pid 81444 (ttys009) alone, one registry record for sessionId 12b4a99c…, no collision. PM cleared the retirement: `EMBASSY_EVENT_TTL_MS=60000 embassy service install` → loopback check to trigger prune → retirement gone → `embassy service install` restored defaults; broker healthy, 26 routes. Side effect: one accepted delivery to swe3 (the emb-136 unit message) was in flight during the reload → receipt likely unconfirmed; swe3 asked to confirm receipt. All-clear sent to infercat-pm [b2e301] with the one-send re-registration step.
- 2026-09-08 RECOVERED: infercat-pm sent once → re-recorded as infercat-pm@m5dev (claude); first message to codex-infercat-swe@m5dev accepted into the engineer's session; PM told the channel is restored. Incident closed; product unit (4.6.0) remains with swe3.
- 2026-09-08 RC 9f96e4d (codex/claude-session-resilience, CI 34200506650 green, check 422/422) → adversarial read HOLD: F1 BLOCKER unvalidated registry name in the duplicate warning breaks the closed projection (status/refresh fail with INVALID_HANDLER_RESPONSE while a duplicate exists; reproduced); F3 BLOCKER revalidation now full-scans and throws on truncation (256+ registry files → every Claude write fails CLAUDE_PEER_TARGET_CHANGED, ~28k retries); F2 MAJOR newest-selection inverts on a probe miss and the hint tells the operator to kill the live session; F4–F8 minors/nits (ps spawn cost ×3, PID_NOT_LIVE overloaded, daemon names published, no note for the control 6→7 bump, probing the held socket). Sound: fencing, TOCTOU, daemon eligibility, envelope bounds, incident e2e test. Sent to swe3 for RC2; live drill deferred to RC2.
- 2026-09-08 RC2 2aed37a (CI 34202824679 green; check 426/426): adversarial re-read → GO. F1 closed (allowedClaude gate + shared normalizeClaudeAlias; exhaustive alias-grammar check), F2/F5 closed (reasons stale_older / unreachable_newest / all_unreachable; destructive hint only when newest is confirmed), F3/F4/F8 closed (exact prepared-record revalidation; UUID-filtered post-connect check; 300-stale-files test; ≤16 probes on one deadline; held socket never probed), F6/F7 closed. Roll-forward NITs: bare 4096 literal vs limit() max; Date.now() in probe budget instead of injected clock; many-to-one alias normalization can create PEER_ALIAS_COLLISION for mixed-case names. Live drill pending a quiet ledger (infercat traffic in flight).
- 2026-09-08 Ledger never quiet (infercat pair in a work loop: 1 accepted + 4 queued for 20+ min). Ruling: one reload instead of two — LAND RC2 on main (0412e8f → 2aed37a) and release 4.6.0 now on check 426/426 + CI green + reviewer GO + the e2e incident fixture; run the live drill (retire-then-send; duplicate process of my own session) during the cut-over reload itself, rollback to 4.5.2 if it fails. infercat-pm warned: queued persists, an accepted delivery will read unconfirmed/CONTROLLER_RESTARTED (do not resend), update its CLI to 4.6.0 (control 7).
- 2026-09-08 RELEASED 4.6.0: release commit 0d26861 on main (fast-forward from 2aed37a), tag v4.6.0, pipeline 34205493536 green incl. npm publish, npm serving 4.6.0; PM re-verified main/tag/npm/release. m5dev cut over (npm + pnpm copies; ledger was empty at the reload moment), broker healthy, 27 routes. Drill B on the live 4.6.0 broker PASSED: retire own route (reg_c6a6d57d…) → one send → fresh id reg_ad3f776a… → delivered. Drill A attempt 1 was void: `claude --session-id <in-use uuid>` produced no second record; attempt 2 void: scratch processes stalled at the trust-folder prompt; attempt 3 running from a trusted dir with the confirm keypress. this-mac unreachable (no route to host) → still 4.5.2; federation protocol unchanged, retry when the laptop is back. Note: the runner brief template still carried the previous session's Claude-Session trailer URL — update the template for the next release.
- 2026-09-08 Drill A (duplicate process) not reproducible by scripted pty: scratch `claude` processes stall at the trust-folder prompt or lack a transcript to resume before a registry record exists (5 attempts). Scenario stands on the e2e fixture (real registry files + sockets, both directions) and the adversarial re-verification; drill B passed live. infercat-pm updated its CLI to 4.6.0 and confirmed a delivery-confirmed round trip with its engineer. swe3 told: released, drill results, three roll-forward NITs. emb-136 CLOSED except: this-mac cut-over to 4.6.0 pending laptop reachability (federation 3 unchanged; its CLI/broker are consistent at 4.5.2).
