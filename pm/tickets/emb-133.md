---
id: emb-133
title: Embassy TUI — engineer-led (contract → RC); PM direction + verification
kind: normal
size: 2
status: dispatched
release: unassigned
updated: 2026-09-06
---

## Binding

Founder (2026-09-06): "implement either a TUI or a macos gui … the TUI might make more sense given that both the claude code and the codex cli live in the terminal." PM direction T1–T5 sent (one command, live status/endpoints/deliveries/queue, existing verbs only, metadata-only, pure client, works over ssh; acceptance = both brokers healthy + retire a stranded endpoint in <1 min without docs).

Engineer contract v1 (2026-09-06): `embassy tui`; no broker state/protocol/semantics change; shows local broker health, local endpoints with queue depth/last operation, remote cached endpoints per host with observation age/safe error, recent deliveries (state/code/age, optional last-hour filter over retained rows), retirements; "not a provider readiness proof" label. Truth boundary: the remote catalog carries no remote broker health or per-endpoint queue/last-operation → shown as "not reported"; run the TUI on the other machine over SSH to see its broker — the ONE narrowing of T5 (proving both brokers healthy from one screen would need new remote reporting). Keys: refresh, loopback check, delivery-token lookup, retire selected LOCAL endpoint with confirmation naming alias + endpoint ID (request uses the exact ID), quit; remote retirement unavailable with an owner-host note; actions call existing control methods and show actual results; no sends; token lookup stays in memory. Poll 1/s, one in flight, actions serialized never replayed; disconnection = visibly stale snapshot + exact error; no auto discovery refresh or SSH probes; non-TTY prints status text once; resize/small windows/control-char escaping/cursor restore handled; Node built-ins + ANSI, no new runtime dependency. Budget: ≤600 runtime lines target, 900 ceiling; ≤700 test lines; one new concept (the command). RC proof: fake-control/terminal transcripts + real CLI/control integration on test-owned brokers; check + CI green; PM verifies the <1-minute flow on both machines. Precondition: engineer characterizes the macOS Node 20 conformance flake first and reports separately if runtime.

## Record

- 2026-09-06 Contract v1 received; PM accepts the T5 narrowing (remote health is not in the catalog; adding it would be a broker responsibility, which the contract rightly refuses). Forwarded to the founder for signature.
- 2026-09-06 FOUNDER SIGNED. Standing changes: "no need to wait for my
  signature going forward. remember the autonomous part from your AIPM
  charter." → the PM signs contracts autonomously and informs. After TUI
  v1: a polishing pass — Opus subagents adversarially review the TUI from
  a user's perspective (ease, intuitiveness, common flows); findings → one
  polish list for the engineer (v1.1).
- 2026-09-06 TUI v1 RC: tui/v1 head 9857e41 on main 2f772e9 (one commit);
  src +321 (tui.ts 307) / tests +266 / docs +28; CI 34054711991 green
  attempt 1 (READ). Flake classified as test scheduling (fixture emitted
  completion in setImmediate against a 10 ms terminal timer); new
  deterministic test; no runtime change. clientInfo.version + CLI now read
  core-version.ts. PM: local check + pty-driven live verification against
  the 4.0.0 broker, then the Opus user-perspective polish pass.
- 2026-09-06 TUI v1 GO + LANDED on main at 9857e41: check 329/329 (PM);
  live pty drive against the 4.0.0 broker — initial render, c/r results,
  masked token prompt with exact-format hint, x→n cancel, q exit 0;
  non-TTY prints status once. PM observations for the polish list:
  result tab shows raw JSON; "Updated Ns ago" kept growing while in the
  result/token modes (polling paused or unchanged snapshot?); endpoint
  IDs not shown in the list. Polish pass (Opus user-perspective
  reviewers) launched.
- 2026-09-06 CI on main at 9857e41 READ: run 34054935137 — all five legs SUCCESS.
- 2026-09-06 POLISH PASS done: two Opus operator reviews (first-time; two-
  machine daily user). Consolidated list sent to swe3 (job tmp tui-polish-
  list.md): MUST P1 sort deliveries newest-first; P2 success codes render
  as faults + fault count in the tab; P3 dead broker headlines "healthy";
  P4 actions freeze the screen (no redraw for up to 15 s) + bare error
  codes without the CLI hint; P5 retirement modal states no consequences,
  cursor defaults to the operator's own session; P6 collision marker
  missing + index-based selection slides under the cursor. SHOULD P7–P13
  (repaint caching, narrow-column priority, SSH-row x message, result-tab
  yank, blind token lookup, loopback residue, local host in title). Flows:
  health/who PASS; recent/stuck FAIL; token FRICTION; retire FRICTION;
  disconnect FRICTION; small ssh PASS. KEEP list stated. v1.1 = one RC.
- 2026-09-06 FOUNDER approved Option A (client-side single pane of glass
  over all brokers in nodes.json via ssh `status --json`; remote actions
  via the remote CLI over ssh; no broker/protocol change). Queued as the
  4.2.0 TUI after v1.1; direction sent to swe3; contract on arrival,
  PM signs autonomously.
- 2026-09-06 codex-embassy-main@m5dev re-registered and asked for work.
  Dispatched a READ-ONLY new-user onboarding audit of 4.0.0 docs (README →
  SKILL → CONFIGURATION → --help; six goals on paper; stale 3.x leftovers;
  command/flag parity). Findings feed the 4.1.0 docs. No overlap with the
  TUI lane.
- 2026-09-06 TUI v1.1 RC: tui/v1.1 head 36a5631 on main 9857e41; P1–P13
  implemented; tui.ts 442 lines (+467/−139 over 6 files); CI 34055876589
  green attempt 1 (READ); engineer check 338/338. PM check + pty drive
  next, then land + 4.1.0 release. Codex agents-view spike dispatched to
  codex-embassy-main (read-only research) alongside its docs audit.
- 2026-09-06 TUI v1.1 GO + LANDED on main at 36a5631 (check 338/338; pty
  drive confirmed P1–P6 + P13 visually). 4.1.0 release next (CI on main
  pending READ), then the multi-host TUI contract.
- 2026-09-06 MULTI-HOST TUI contract v1 SIGNED by the PM (autonomy): host
  overview + per-host detail pane; local 1 s control polling; remote
  panes via the same fixed ssh/non-interactive/env-allowlist as
  federation, ~5 s cadence, one in flight per host, 8 s wall timeout,
  bounded output, stderr never rendered; remote verbs = existing CLI with
  closed grammars; retirement captures HOST + full ID; lost action replies
  = unknown outcome, no replay; unsupported shapes never guessed (lazy
  `--version` observation labelled CLI metadata); non-TTY stays a single
  LOCAL snapshot with no ssh; budget 250–300 target / 450 ceiling / tests
  ≤500; base main 36a5631, rebase onto the 4.1.0 release commit.
- 2026-09-06 Onboarding audit (codex-embassy-main) received: 10 findings
  (Codex receiver prerequisite unstated; upgrade order lacks the launchd
  stop/install/start sequence; no two-Mac recipe; @studio vs default host
  + state-path disagreement; Claude→Codex round trip absent from the
  quickstart; skill handoff unexplained; token lookup needs the retained
  send result; installed-4.0.0 help lacks tui — parity, resolves with
  4.1.0; "service start" implied; "no shell" too broad). Fixes dispatched
  to the same seat as one docs commit on docs/onboarding (1–7, 9, 10; tui
  paragraphs untouched); ships in the release after 4.1.0.
- 2026-09-06 CI on main at 36a5631 READ: run 34056034604 — all five legs SUCCESS.
  4.1.0 runner launched.
- 2026-09-06 FOUNDER on Codex agents view: registration-from-inside is
  the status quo and fine; automatic discovery would be "amazing" UX;
  authorized cloning the open-source Codex CLI for a source investigation
  (done: job tmp codex-src @ ad931a4; Opus investigator running);
  token-spend risk accepted (trusted agents on both sides); NO "discovered
  vs registered" distinction (ceremony); product shape: discovery
  REPLACES registration, register-codex becomes the fallback for harnesses
  without native integration. Next line after multi-host TUI: "Codex
  discovery" contract from the investigator's + main's findings.
- 2026-09-06 RELEASE 4.1.0: runner completed steps 0–5 (commit e32d1c7,
  tag v4.1.0 pushed, main = e32d1c7; check 338/338); step 6 release
  pipeline 34056251234 attempt 1 FAILED macOS Node 20 on
  test/claude-peer.test.ts:1318 "a post-connect timeout is ambiguous
  rather than not-written" (timing; CI on the same commit passed twice:
  runs 34056240750, 34056251182). PM decision: one rerun of the failed leg
  (tag cannot move; dead tag if it fails again → 4.1.1). Flake sent to
  swe3 for a deterministic fix before the 4.2.0 RC. Second distinct
  macOS Node 20 timer race in the safety corpus.
- 2026-09-06 Codex agents-view spike (codex-embassy-main, installed 0.153.4,
  schema export): agents view = App Server threads on the shared local
  daemon (`codex agents`, also --remote); thread/resume by ID + turn/start
  works without a foreground process (non-ephemeral threads); subagents
  are independent UUIDv7 thread IDs (parent id, nickname, role in
  SubAgentSource); thread/list paginated with sourceKinds (default =
  interactive only — must include exec/appServer/subAgent…), archived
  separate, useStateDbOnly for observational listing; thread/loaded/list;
  ThreadStatus notLoaded/idle/active/systemError; notifications thread/
  started, status/changed, name/updated, archived, deleted, closed; names
  mutable and non-unique (thread/name/set) → identity = thread UUID
  (private), alias = name/nickname with collision-safe selector;
  canAcceptDirectInput experimental; 30-min in-memory unload is a grace
  period, not disk retention; parent archive/delete cascades. Feasible at
  the protocol layer without new App Server methods; product rulings
  needed on admission scope (which sourceKinds), collisions, dormant
  threads. Source investigator's report pending → then the contract.
- 2026-09-06 RELEASE 4.1.0 DONE: release run 34056251234 attempt 2 SUCCESS
  (all seven jobs); GitHub release published; npm serves 4.1.0; tag
  v4.1.0 = e32d1c7 = main. CUTOVER: m5dev reinstalled from npm (npm +
  pnpm copies), broker healthy, loopback ok, installed help lists tui.
  this-mac cutover PENDING: the laptop became unreachable over ssh
  (hostname no longer resolves) — it stays on 4.0.0 until it is back;
  this doubles as the multi-host TUI dead-host scenario.
- 2026-09-06 Codex SOURCE INVESTIGATION (Opus, clone @ ad931a4): agents view
  = App Server threads (thread/loaded/list + thread/list + broadcast
  notifications; agent == thread; agent_id IS a thread id); wake = thread/
  resume (excludeTurns) then turn/start (auto-steers an active turn);
  output persisted regardless of subscribers; no "delivered" concept;
  sub-agents = UUIDv7 threads that survive the parent (edges in state_5
  .sqlite thread_spawn_edges); multi-agent V2 spawned sub-agents REFUSE
  direct input (canAcceptDirectInput=false; model-declared); default
  thread/list source filter excludes exec/appServer/subAgent kinds; auth =
  0600 UDS + self-asserted clientInfo — NO per-thread owner to bind (bind
  thread.id only; fork → new id); every initialized connection is auto-
  subscribed to every new thread and subscribers pin threads loaded
  (unload after 60 s idle with zero subscribers → thread/unsubscribe);
  daemon = ~/.codex/app-server-control/app-server-control.sock, started
  by `codex app-server daemon start` (not launchd), hourly self-update
  restarts it (60 s drain); embedded in-process TUIs are invisible on the
  socket; approvals on a broker-started turn need a policy or an
  answerer; nothing needed is HEAD-only (strings verified in 0.153.4).
  Feasible for ordinary threads without registration; not for V2
  sub-agents. Full report: job tmp codex-discovery-investigation.jsonl.
- 2026-09-06 MULTI-HOST TUI GO + LANDED on main at 11293d0 (check 352/352;
  CI 34056751682 green; live dead-host drive: overview, [ / ], this-mac
  pane STALE !PEER_TUNNEL_UNAVAILABLE with advice, local pane still 1 s,
  retire refused on the dead pane, q exit 0). 4.2.0 publish HELD until the
  laptop returns for the live two-host remote-retire check. Codex
  discovery direction (D1–D8) sent to swe3 for its next contract.
- 2026-09-06 Onboarding docs FROZEN at 56fb5ac (from 36a5631; +184/−45 docs,
  2 test pins; CI 34056919473 green). Main is at 11293d0 → asked for a
  cherry-pick onto main as docs/onboarding-2; lands on the new head.
- 2026-09-06 CI on main at 11293d0 READ: run 34056995503 — all five legs SUCCESS.
- 2026-09-06 claude-peer post-connect flake classified by swe3 as a TEST
  race (fixture equated fake connect with write start; the 10 ms timer
  could win during real async revalidation, where CONNECT_TIMEOUT/
  recoverable is correct). Deterministic tests with controlled timers; no
  runtime change. Landed on main 11293d0 → cbdd4a2 (CI 34056976764 green).
  PM slip: the record append ran in the gate worktree and made a stray
  commit on its detached HEAD (push to the records branch was rejected,
  nothing leaked); worktree reset to cbdd4a2 and cleaned. Note: main's
  tree carries tracked pm/tickets/emb-49..62.md, so `rm -rf pm` there
  deletes tracked files — restored.
- 2026-09-06 CODEX DISCOVERY (4.3.0) contract v1 SIGNED: daemon-backed
  agents appear without registration within ~5 s (bounded thread/list
  with six source kinds + loaded list + lifecycle/name notifications,
  subscribe-before-reconcile); register-codex = fallback, same endpoint
  kind, no badges; UUID private/immutable, fork = new identity, names →
  safe alias grammar with ambiguous collisions, closed ≠ retired; resume
  (excludeTurns, no overrides) → operation with the Embassy turn trigger;
  direct-input capability checked at the boundary, V2 children visible
  with the exact refusal; ordinary messages queue while busy, only
  explicit STEER steers (confirmed); subscriber hygiene; no daemon
  launch/update; optional Codex metadata projection + one private-control
  bump, no schema change; budget ≤900 (cap 1,400) runtime / ≤1,800 tests;
  base = main after 4.2.0. Investigation report handed over as
  /tmp/codex-discovery-investigation-report.md.
- 2026-09-06 (late-completing cutover command: this-mac ssh was refused on
  port 22 — the laptop has no inbound SSH; m5dev-only cutover stands.)
- 2026-09-06 Laptop awake (founder mis-remembered: inbound ssh works on the
  lab subnet). this-mac cut over to 4.1.0 (healthy, loopback ok). LIVE
  TWO-HOST CHECK ✓ from m5dev on the multi-host build (cbdd4a2 dist):
  both panes healthy, ] to this-mac, its stranded endpoint codex-
  stranded@this-mac selected, x → confirmation with Host + full ID +
  consequences, y → "retire result ready: cancelled 0/ambiguous 0/
  unconfirmed 0", polling paused note, q exit 0; this-mac's own ledger
  lists the retirement and no routes. Dead-host half verified earlier.
  Multi-host acceptance COMPLETE. CI on main at cbdd4a2 green (run
  34057088720). 4.2.0 runner launching.
- 2026-09-06 Onboarding docs LANDED: PM cherry-picked 9aad1a2 onto main cbdd4a2 → 2d7195b (clean; check 353/353); ships in 4.2.0. CI on main pending READ.
- 2026-09-06 CI on main at 2d7195b READ: run 34057928462 — all five legs SUCCESS. 4.2.0 runner launched.
- 2026-09-06 RELEASE 4.2.0 DONE by the runner end to end (push permitted
  this time): commit 2eee06d = tag v4.2.0 = main; pipeline 34058163266
  SUCCESS attempt 1; GitHub release published; npm 4.2.0; check 353/353.
  CUTOVER: both machines on 4.2.0 (m5dev npm + pnpm; this-mac nvm),
  brokers healthy, loopback ok. Runbook step 2 wording fixed to
  core-version.ts. Main cleared for Codex discovery (4.3.0) at 2eee06d.
  TUI line CLOSED (v1 → v1.1 polish → multi-host). PM slip repeated once
  more (record ran in the gate worktree; stray commit discarded, nothing
  pushed) — every record command now begins with cd tickets-sync.
- 2026-09-06 Codex discovery BLOCKER ruled: turn/start auto-steers an
  active turn and the App Server offers no atomic idle-only start
  (turn_processor.rs:638–658; serialization is per request). Ruling:
  idle check immediately before turn/start (same check the registered
  path relies on today), ordinary messages queue in the ledger while
  active; a lost race (Started vs Steered in the response) is DETECTED
  and settled with an explicit outcome code + journaled, never silent,
  never replayed; documented as a one-sentence limitation; explicit
  STEER keeps turn/steer with expected turn id; no thread/queue/*; no
  upstream dependency. Contract wording amended ("never intentionally").
- 2026-09-06 Correction: the App Server's turn/start response carries no
  Started/Steered discriminator (turn_processor.rs:660–700 builds the same
  Turn for both) — my "detect and report" item withdrawn; no manufactured
  outcome code. FINAL: best-effort idle gate + explicit STEER; the
  residual race is documented as undetectable (receipt = acceptance and
  lifetime, not a fresh-turn guarantee); same for the registered path.
- 2026-09-06 Founder shared the real agents view (Working/Ready/Finished;
  19 finished = old unrelated tasks). Ruling sent to swe3: mirror the
  view's own bound/order (recency top 20 root threads) rather than the
  whole thread store; bucket → Embassy status: Working=busy, Needs
  input=waiting, Ready=idle(loaded), Finished=dormant(resume then start);
  unnamed threads get generated typeable aliases.
- 2026-09-06 FOUNDER: "embassy doesn't ever need to worry about how to
  address a subagent - only head agents are ever in the picture." Codex
  discovery scope = ROOT threads only; sub-agent kinds, child grouping and
  the V2 direct-input refusal surface removed from the contract. Sent.
- 2026-09-06 Codex discovery RC1 dcdd7e2 (CI 34059730789 green; check
  381/381; runtime +998 over the 900 target, under the 1,400 cap; one new
  refusal code CODEX_DIRECT_INPUT_UNAVAILABLE; control 5→6; schema 6
  kept). Built before the founder's root-only and top-20 rulings arrived
  → HOLD for RC2 (delete sub-agent kinds/grouping/parent refs/V2 surface
  unless roots can produce it; mirror the agents view's recency top 20).
  PM will run the Opus read on RC2 (engineer's review agent ran out of
  compute), then the live checks.
- 2026-09-06 Codex discovery ruling: private persisted retention marker
  authorized (explicit registrations survive the top-20 window; automatic
  rows list only while in it); schema 6→7 additive, forward-only read of 6
  (existing rows ⇒ retained), refuse ≤5, no reset; rollback limit: 4.2.0
  refuses schema 7 (restore backup). Window aging ≠ retirement (no
  suppression; identity/settlement preserved; re-listing as the same
  endpoint). Control bump 5→6 unchanged. RC2 to follow.
- 2026-09-06 Codex discovery RC2 8bc1fa5 (base 2eee06d): roots only,
  recency top 20 (limit 20 / recencyAt / archived false / cli,vscode,exec,
  appServer), sub-agent code deleted, private retention marker, schema 7
  additive with forward-only read of 6 (existing rows retained), control
  5→6, federation 3; runtime +881/−47 (under the 900 target), tests
  +827/−18, docs +203/−90; CI 34061577676 green attempt 1 (READ); PM check
  383/383. Opus independent read running; live drill plan staged.
- 2026-09-06 RC2 Opus read: HOLD. BLOCKER sortKey "recencyAt" (wire =
  "recency_at") → discovery would never work on real metal; MAJORs:
  broker.refresh couples Codex failures to Claude/SSH refresh; aged-out
  automatic rows stay in the ledger invisibly → 128-cap exhaustion with
  PEER_ALIAS_COLLISION on every codex-* selector; resume "no rollout
  found" not mapped to THREAD_NOT_OBSERVED. Root filter, retention/schema
  7, enrich-on-list all verified sound. Sent; RC3 expected.
- 2026-09-06 Codex discovery RC3 a1f6462: the four RC2 items fixed (recency_at
  with a fixture copied from the codex binding + mutation check; refresh
  decoupled; unused automatic rows pruned before the cap with pending
  deliveries preserved and a NEW id on reappearance; resume missing/
  archived → THREAD_NOT_OBSERVED); runtime +890/−48, tests +948/−18; CI
  34062593866 green attempt 1; PM check 392/392. Delta Opus read running;
  live drill staged (tarball + backups on both machines).
- 2026-09-06 RC3 delta read: GO (all four fixes CLOSED; pruning stress-
  tested: serialized transactions, pending phases exempt, reply to a
  pruned-then-returned root refuses ROUTE_BINDING_MISMATCH; fixture
  byte-identical to the codex binding). LIVE DRILL on both machines
  (RC3 installed; state 6→7 forward, 3 prior registrations retained):
  PASS parity (19 roots m5dev / 18 this-mac after starting its daemon;
  names match the founder's view), dormant wake both machines (replies
  reached the PM session natively; laptop by discovered name across
  federation), idle gate + STEER (3 inside the turn, normal after),
  retire without resurrection, daemon restart on this-mac (aliases
  identical). FAIL fallback: register-codex --alias codex-fallback was
  renamed by the next reconcile to the generated alias (retained row
  kept, alias lost; send by the registered alias → ROUTE_UNREGISTERED)
  → MAJOR → RC4 (retained rows keep their registered alias). Minors
  from the read folded into the RC4 ask.
- 2026-09-06 RC4 5542fc1 (one-line fix: retained rows keep their registered
  alias + regressions; OBSERVER_BACKPRESSURE code; docs clauses; test
  timeout): check 394/394; CI 34063755304 green attempt 1; LIVE fallback
  re-check on m5dev PASS (alias survived two scans; delivery by the
  registered alias DELIVERED). Codex discovery GO → LANDED on main
  (2eee06d → 5542fc1). Both machines on the RC4 build; drill endpoints
  retired. 4.3.0 release next.
- 2026-09-06 CI (workflow CI) on main at 5542fc1 READ: run 34063959560 — all five legs SUCCESS. (First lookup returned the Pages deploy run; brief corrected.) 4.3.0 runner launching.
- 2026-09-06 4.3.0 runner stopped at the step-2 grep on three legitimate "4.2.0" mentions (SECURITY rollback limit, a ledger-codec comment, the README compatibility line); ruled allowed, resumed at step 3.
- 2026-09-06 RELEASE 4.3.0: runner completed steps 0–5 (commit 06f5a94 on 5542fc1; check 394/394); `git tag` blocked for the runner → PM tagged v4.3.0 = 06f5a94 and pushed main + tag. Pipeline watch running (steps 6–8).
- 2026-09-06 RELEASE 4.3.0 DONE: release run 34065052531 SUCCESS attempt 1
  (all seven jobs); GitHub release published; npm 4.3.0; tag v4.3.0 =
  06f5a94 = main. CUTOVER: both machines reinstalled from npm (m5dev npm
  + pnpm; this-mac nvm), brokers healthy, loopback ok, discovery complete
  on both (m5dev 18 roots; this-mac 22 endpoints). CODEX DISCOVERY LINE
  CLOSED. Day tally: 4.0.0, 4.1.0, 4.2.0, 4.3.0 released; both machines
  on 4.3.0. Open: founder's next direction; codex-embassy-main standing
  by; the Codex daemon must be running on a machine for discovery
  (Embassy never starts it — documented).
