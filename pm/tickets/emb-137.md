---
id: emb-137
title: Hotfix — registry names outside the alias grammar ("name (2)") must not invalidate a Claude session
status: active
opened: 2026-09-19
---
- 2026-09-19 INCIDENT (founder relayed from session compressor-pm): every send from that session fails REGISTRY_INVALID_SCHEMA; the session proposed moving gateway-state.json aside (a full ledger wipe). PM diagnosis on 4.6.0: broker healthy; my sends work; sending as socket 15437 ("compressor-pm") works; sending as socket 99673 ("compressor-pm (2)") fails REGISTRY_INVALID_SCHEMA. Cause: Claude Code auto-renames a second same-named session to "<name> (2)" (nameSource "peer"); parseRegistryRecord rejects names failing ALIAS_PATTERN (space/parens) → record invalid → cannot send, not discovered. Also affects "infercat-pm (2)" and "bunny-health-main (2)". Ledger wipe vetoed (destructive to every session; irrelevant to the cause). Hotfix dispatched to swe3 as 4.6.1: accept any bounded registry name, alias via normalizeClaudeAlias; tests + OPERATIONS line. Stuck session told to hold (no retirements/edits/wipes) and retry ticket 2 after the upgrade.
- 2026-09-19 Founder renamed the stuck session to a plain name; PM verified: registry parses, discovered as compressor-pm@m5dev, send as its socket delivers; retry instruction delivered to it natively via Embassy. Hotfix RC 129c239 (codex/claude-display-name-hotfix on 0d26861; CI 35422090579 green; check 429/429): ALIAS_PATTERN dropped for registry names (non-empty, ≤64, no NUL), alias via shared normalisation with punctuation runs collapsed and a deterministic claude-<hash> fallback; fixture sends both ways as "compressor-pm (2)" and a punctuation-only name. LANDED main 0d26861 → 129c239. 4.6.1 brief/notes drafted; main CI pending → runner → cut over both machines → confirm "infercat-pm (2)" and "bunny-health-main (2)" become discoverable.
- 2026-09-19 compressor-pm confirms ticket 2 delivered + ACKed (incident closed on its side). Stale second compressor-pm@m5dev endpoint (reg_a241c3ea…, dead duplicate session, zero pending) retired by id; alias resolves to the live endpoint alone. 4.6.1 release pending main CI.
- 2026-09-19 RELEASED 4.6.1: release commit 90e575e on main (fast-forward from 129c239), tag v4.6.1, pipeline 35422416269 green incl. npm publish, npm serving 4.6.1; PM re-verified main/tag/npm. this-mac cut over to 4.6.1 (healthy, loopback ok). m5dev cut-over waits for compressor-pm's in-flight delivery to its engineer to settle (one accepted + one queued), then reload + confirm the two remaining renamed sessions are discoverable.
