---
id: emb-132
title: v4 — network-of-agents core (ENGINEER-LED; PM gives direction and verifies the RC)
kind: normal
size: 3
status: dispatched
release: unassigned
updated: 2026-09-05
---

## Binding

DRAFT — relayed by the engineer (GPT-6 Astra) from the founder's Codex seat on 2026-09-05; the founder has not yet confirmed in the PM session. Proposal: a simpler, more robust steady state where many Claude and Codex agents message each other locally and across user-owned machines through SSH gateways; STEER explicitly kept.

Proposed contract (engineer's, PM-annotated): keep native incoming delivery/wakeup into Claude and Codex, STEER, exact session identity, provenance, bounded queues/backpressure/deadlines, per-operation attestation, uncertainty/no-replay, identity-bound explicit replies, SSH auth + destination-owned handoff, truthful status, OS-supervised startup. Support all four provider pairs incl. same-provider cross-host (send() at service.ts:1354 currently refuses Claude→Claude / Codex→Codex). Agent-originated sends standardize on the Embassy CLI; DROP per-route native Claude ListAgents/SendMessage advertisements + helper/listener/native-receipt UX (PM: STRONG PUSHBACK — founder decision #1: is `embassy send` from Claude acceptable as the Claude-side gesture, or is native SendMessage the headline?). Drop automatic Codex output forwarding (agents reply explicitly; STEER + turn tracking kept). Drop shell-peer user endpoints/token/await/ephemeral-check (PM: MEDIUM PUSHBACK — keep a minimal loopback self-test; founder decision #2). Reduce counters/analytics/journal to the recent ledger + retirement evidence.

Proposed core: (1) one endpoint directory — opaque IDs are identity, aliases are lookup indexes (rename = one row; today's fan-out store.ts:2160 / service.ts:1620); (2) explicit registration for both providers, discovery/attestation kept, no competing route maps; (3) one ledger + one delivery coordinator keeping queued/reserved/armed/accepted/terminal; three explicit adapters (Claude socket, Codex operation, SSH handoff); (4) remote catalogs = bounded in-memory caches, destination-owned queues, never silent re-resolution; (5) replies derived from the ledger, refusal on retired/replaced identities; (6) shrink the state model first, keep bounded JSON, extract a pure transition core afterwards. Deletion candidate: onClaudeReply / pending-reply / tombstone machinery (no production producer; proof required). Size: ~19.4k src today; 14–16k credible first target; no number chased.

First deliverable on confirmation: signed minimum contract + an offline reference-core prototype/test plan proving all four pairs, local/remote delivery, busy/STEER, rename/replacement, restart, uncertain writes — before any cutover. Reset/migration policy explicit. E5 negotiated.

## Record

- 2026-09-05 Drafted from the engineer's relay; PM position sent (two pushbacks, otherwise agreement); dispatch HELD for founder confirmation in the PM session. Sequencing: after 3.1.0 release + cutover.
- 2026-09-05 FOUNDER (PM session): "this should be an engineer-led effort …
  what i want you to do is to give overall direction and act as the
  verifier once the eng gives you a rc". Work model for v4: engineer owns
  tickets/slices/design/source; PM gives direction (G1–G7 sent to swe3:
  four pairs + native wake, one-step Claude gesture as the engineer's
  call with an acceptance test, STEER kept, safety invariants byte-
  tested, truthful status + retirement + supervised startup + a loopback
  self-test, fewer responsibilities not a tidier copy, explicit reset
  policy) and verifies the RC end to end on both machines. Deliverables
  requested in order: contract (founder signs) → engineer's slice plan
  (PM mirrors to Linear) → offline reference-core prototype checkpoint →
  RC. v4 starts after the 3.1.0 cutover.
- 2026-09-05 FOUNDER: no tickets or slices at all for v4 ("too much
  ceremony … who is the audience? no one"). v4 artifacts = the one-page
  contract (founder signs) and the RC (PM verifies against G1–G7). This
  ticket is the only v4 record and will hold the contract and the RC
  verdict; no Linear mirroring of v4 work.

## Contract v1 (engineer, 2026-09-06) — forwarded to the founder for signature

Goal: reliable named communication among Claude Code sessions and Codex CLI tasks on user-owned machines — all four provider pairs, locally and over direct SSH; receivers wake natively; after onboarding, an agent told only a peer's name sends in one command and receives an explicit reply without operator help.
Start/ownership: main cleared at 0b6c369; code only after signature, on a new lane; engineer owns implementation, PM verifies the RC; only two artifacts (contract, RC).
Keep/simplify/remove: CLI sending (`embassy send` resolves the caller from inherited identity), native receiving (Claude socket, Codex turns); REMOVE native Claude SendMessage/ListAgents advertisement helpers; STEER stays with exact active-operation ownership and lifetime tracking; catch up in ONE wake (emb-131) with bounded backlog, identity/provenance/ordering/receipt preserved; one directory + ledger + coordinator, three adapters (Claude socket, Codex operation, SSH handoff); opaque endpoint IDs are identity, aliases are indexes; bounded JSON initially; remote catalogs are caches; destination owns its queue; first contact must not wait for a catalog poll — source identity attested by its SSH-authenticated owning broker; explicit replies via ledger-bound endpoint identities surviving restart while valid, never silently re-resolved, references are not credentials; truthful status, operator retirement, recent retirement evidence, OS-supervised startup, one-command loopback self-test through real admission/ledger/coordinator/receipts; REMOVE shell-peer user endpoints/token/await UX, automatic Codex-output forwarding, unproduced reply machinery (under proof), lifetime counters and general analytics/journal.
Safety/evidence: exact identity, provenance, per-write attestation, bounded queues/deadlines/backpressure, queued/reserved/armed/accepted/terminal; never replay uncertain writes or retarget old identities; receipts = transport evidence; retained safety corpus carried onto the new path incl. all pairs, federation, catch-up, STEER, rename/replacement, first contact, restart, uncertainty.
Upgrade: reset-only (inspect/drain or settle old work, backups preserved, incompatible state refused without mutation, rollback limits documented). No perpetual migration machinery.
Budget: ≤6,000 added src lines + ≤6,000 added test lines (moves count); deletions uncapped, justified by removed responsibilities. Direction sought only for budget excess or contract change. RC = frozen head, green CI, G1–G7 evidence, reset instructions; PM verifies both machines.

PM read against G1–G7: meets all seven. The one explicit product trade-off for the founder: native Claude→Codex SendMessage is REMOVED — Claude sessions send with `embassy send` (one command; the CLI resolves the caller), and still receive natively. Everything else is strictly a simplification. PM recommends signing.
- 2026-09-06 FOUNDER SIGNED contract v1. Notes verbatim: "honestly i think
  this effort should result in substantial net deletions but the 'effort
  ceiling' works for me." and "'Claude sessions will send with embassy
  send' this is acceptable to me. native receiving is more important than
  native sending because the former gives us the right lifecycle behavior
  for free." Relayed to swe3; implementation starts on a new lane from
  main 0b6c369. PM verifies the RC against G1–G7 with native receive/wake
  as the hardest-checked invariant and net deletion as the expectation.
- 2026-09-06 FOUNDER: "encourage the engineer to implement everything
  efficiently - do not waste time with unnecessary ceremony - do away
  with formalities if they do not contribute to forward progress."
  Relayed: no reports/accounting/freeze rituals until the RC; message
  the PM only on contract-changing decisions or blockers; terse RC
  handoff (head, CI, G1–G7 shown true, reset steps).
- 2026-09-06 RC RECEIVED: v4/core head 0bc8e9c (base 0b6c369, 6 commits);
  runtime 9,482 vs 19,398 lines (PM-counted; net −9,916); added src 3,852
  / tests 3,901 under the 6k/6k ceiling; version 4.0.0-dev; schema 6,
  federation 3; CI 34010355600 green attempt 1 (READ). PM local check +
  one Opus adversarial read running; live two-machine drill next if GO.
- 2026-09-06 Opus RC read: GO for the live drill (1 MAJOR docs: site/
  index.html teaches v3 flags; MINORs: peer host self-asserted at
  initialize (federation.ts:603–606) — provenance forgeable by nodes.json
  misconfiguration, ARCH:193 overstates; shared 500-row rate table;
  CLAUDE_PEER_TARGET_CHANGED terminal post-auth; ambiguous-by-exception
  pre-write; escaped-body frame cliff; receipt eviction hides settled
  tokens; retirement fence capped at 500; clientInfo 1.7.0). Live cutover
  to 4.0.0-dev done on BOTH machines (reset-only; v5 state backed up as
  gateway-state.v5-pre400.backup.json on each; nodes.json kept); both
  brokers healthy; `embassy check` loopback ok on m5dev.
- 2026-09-06 LIVE DRILL results so far (both machines on 4.0.0-dev):
  loopback `embassy check` ok on both; a headless `codex exec` task on
  this-mac (never seen by m5dev) registered (reg_65c5f614…) and sent to
  embassy-pm@m5dev in one command with no identity flags → DELIVERED,
  this Claude session woke natively (G1 cross-host Codex→Claude, G2
  one-command, first contact without a catalog poll); identity-bound
  reply `send --conversation` from m5dev → PEER_HANDOFF_CONFIRMED at the
  source, DELIVERED at the destination (G1 Claude→Codex cross-host).
  Status surfaces are truthful (self-test deliveries/retirements shown).
  NIT: this-mac route row shows state null for the probe endpoint.
- 2026-09-06 LIVE DRILL (cont.): local Codex→Claude via a headless probe
  DELIVERED with native wake; local identity-bound reply accepted.
  Headless probes spawned from the PM's Claude session first hit
  CALLER_IDENTITY_CONFLICT with an exact remedy (env -u
  CLAUDE_CODE_MESSAGING_SOCKET) — truthful, G5. ONE-WAKE CATCH-UP: 4
  normal + 3 STEER accepted against a 90 s-busy Codex task (queueDepth 7,
  lastOperation deferred/ROUTE_BUSY), the 4th STEER refused QUEUE_FULL at
  admission (cap 3); when the turn ended all 7 delivered within ~1 s of
  each other with individual receipts, FIFO — G1/G6 catch-up ✓. Watchlist
  3 confirmed live: 12 000 control chars accepted then failed
  PROVENANCE_ENVELOPE_TOO_LARGE (MINOR); 17 KiB refused MESSAGE_TOO_LARGE
  at admission ✓; remote endpoint id retire refused ROUTE_UNREGISTERED ✓.
  STEER into an ACTIVE turn not exercised live yet (a `codex exec` turn
  is not steerable from outside); needs swe3's interactive session or
  rests on the offline corpus.
- 2026-09-06 LIVE DRILL (cont.): CRASH — 3 messages queued behind a busy
  task, broker SIGKILLed: rows survived on disk and read back as queued
  (delivery-status found:true, pendingFor ~160 s); after recovery all 3
  delivered exactly once (G4 durability + no replay ✓). RELAUNCH — launchd
  did NOT relaunch after SIGKILL (state not running for >100 s; recovered
  with `service install`), but DID relaunch after SIGABRT (runs 2, new
  pid, healthy): "restarted after a crash" is true for crash signals and
  false for an operator's kill -9 — docs precision item, pre-existing
  from v3's plist (KeepAlive Crashed). RETIRE DURING TURN — 2 queued rows
  cancelled (2/0/0), endpoint gone, retirement journaled, send to the
  retired alias → ROUTE_UNREGISTERED, second retire → ROUTE_UNREGISTERED;
  turn-uninterrupted evidence pending from the probe transcript. Probe
  endpoints retired on both machines. NITs: status row says "reserved"
  where delivery-status says "queued" for the same row; busy code shown
  as THREAD_NOT_OBSERVED for a `codex exec` task vs ROUTE_BUSY earlier.
- 2026-09-06 Retire-during-turn: the probe's turn ran to completion
  (TURN-FINISHED after the full 70 s) while its endpoint was retired and
  its 2 queued rows cancelled — retirement does not interrupt an active
  turn ✓. RC VERDICT drafted (job tmp v4-rc-verdict.md): CONDITIONAL GO;
  must-fix before tag = self-asserted peer host at federation initialize,
  site/index.html v3 examples, crash-relaunch claim precision; should-fix
  = TARGET_CHANGED post-auth terminal, escaped-body admission bound,
  receipt/retirement retention caps, shared rate table, ambiguous-by-
  exception; remaining live check = STEER into an active turn with swe3.
  Delivery of the verdict is blocked: swe3 has not re-registered on the
  v4 broker (reset dropped its route) and cannot be reached by Embassy
  until it does — founder nudge requested.
