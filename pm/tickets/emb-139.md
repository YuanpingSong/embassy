---
id: emb-139
title: Explicit re-registration must lift a Codex self-retirement (twin of emb-136)
status: active
opened: 2026-09-19
---
- 2026-09-19 INCIDENT (founder, laptop): Codex agent codex-career-helper@this-mac retired its own route (cancelling 4 queued messages from career-orchestrator), then every `register-codex` (incl. --succeeds) returned ROUTE_UNREGISTERED; the concurrent LOCAL_APP_SERVER_NOT_RUNNING was a real transient the user cleared by restarting the app server. Cause: ledger.register fences the Codex nativeKey for retentionMs after retirement (kept deliberately in 4.6.0 so retire sticks against automatic discovery). Laptop broker showed health degraded / ROUTE_UNREGISTERED from the failed operations; pending 0. PM cleared the fence (EMBASSY_EVENT_TTL_MS=60000 reload → loopback prune → defaults) on the laptop broker; career-orchestrator told which messages to resend after the helper re-registers. Unit to swe3 (4.7.0): explicit register-codex from the same live thread lifts the fence with a fresh id (discovery fence stays; old refs fenced), --succeeds of a retired predecessor likewise, retire hint states consequences, skill/README "never retire your own route".
- Pattern across today: three of four incidents were agents retiring their own route or guessing an alias; the codes (ROUTE_UNREGISTERED, INVALID_GATEWAY_CONFIGURATION) named neither the cause nor the way out. 4.6.1/4.6.2/4.7.0 close them one by one.
- 2026-09-19 After the fence clear the helper re-registered; the orchestrator's resend failed RPC_REJECTED with the route dormant (dlv_KweNXY0v…). Broker log holds no RPC error text (safe codes only). Likely: App Server restarted while the helper's TUI stayed open → new daemon sees the thread dormant on disk, old TUI holds it, resume refused. Recovery relayed: restart the helper session so it attaches to the new daemon, re-register, resend. swe3's 4.7.0 clarifications confirmed (busy accepted for the registration gate; --succeeds only with matching native key; retire copy both directions; Codex-only discovery suppression) + added: bounded redacted debug detail for RPC_REJECTED (JSON-RPC code + failing method) and a hint.
- 2026-09-19 Root cause of the RPC_REJECTED: no Codex TUI exists on this-mac; the helper runs inside the ChatGPT desktop app's code-mode host (pid 81398, since 04:05). The managed App Server daemon sees its thread only on disk (dormant) and its resume is refused because the ChatGPT host owns the thread; outbound works (shell only). Not a wake bug and not a TUI-restart case. Advice sent: run the helper as a Codex CLI session (attaches to the managed daemon) or treat it as send-only. Add to 4.7.0: document supported Codex hosts for inbound (managed App Server daemon: CLI TUI, exec; ChatGPT app / other hosts = send-only) in README/SKILL, and an RPC_REJECTED hint that names this case alongside the daemon-restart case.
- 2026-09-19 Follow-on (laptop): helper's report send refused PEER_ALIAS_COLLISION — two live Claude sessions named career-orchestrator (pid 9501 interactive since Sep 17, sid 09961022; pid 97880 bg job since 13:05, sid e3ebbaa2). Founder chose to keep e3ebbaa2. PM retired the old ledger row by id (zero pending) — collision persisted because live discovery still saw both processes; founder could not find the old terminal and asked for termination → SIGTERM to 9501 (verified identity first), exited cleanly. Registry now shows one career-orchestrator; named cross-host send delivered (PEER_HANDOFF_CONFIRMED). Lesson for docs: a name collision is decided by live discovery, not the ledger — exit or rename the duplicate session; retiring rows alone does not clear it.
- 2026-09-19 4.7.0 RC cbfd80e (codex/codex-retirement-recovery on 15792c1; CI 35457672129 green; check 442/442 here): explicit same-thread register-codex lifts a retirement after a fresh loaded/live App Server observation (no resume/start/steer), atomic commit, new endpoint id, automatic discovery still suppressed, old refs fenced, --succeeds requires matching native key; RPC_REJECTED detail (numeric code + method) in memory only with resume-vs-turn guidance; docs on receiving hosts and self-retirement; private control 7→8. Adversarial read running; live drill script prepared (scratch codex exec thread: register → retire → automatic scan → re-register → deliver); 4.7.0 brief/notes drafted.
- 2026-09-19 4.7.0 RC adversarial read → HOLD on F1: the RPC_REJECTED hint has no command gate, so a refused recovery attestation prints resume advice ("restart that session") that would make the retired thread unrecoverable; F2 no capability gate on thread/loaded/list; F3 SECURITY overstates "same-thread" as an enforced boundary; F4 retired --succeeds matches the last row by alias, not the caller's key; F5 retire stderr "unconfirmed" vs possible "ambiguous"; F6 attestation drift keeps the session / capacity reported as PROTOCOL_ERROR. Sound: no write/wake, no resurrection, no retarget, TOCTOU, closed detail, protocol gate. Sent to swe3 for RC2.
- 2026-09-19 4.7.0 RC2 6190a08 (CI 35458682883 green; check 444/444): hints gated by command (register-codex → attestation hint "do not restart the session"; delivery commands → resume/turn guidance), CODEX_ATTESTATION_LIMIT for the bounded scan, 0.153.4 documented as verified baseline, SECURITY wording, --succeeds matches alias+caller key across rows, retire warns ambiguous/unconfirmed, attestation drift disconnects. PM read the gated hints in core-cli.ts:98-100. Reviewer re-attack running.
- 2026-09-19 RC2 re-attack → GO (F1–F6 closed; N1 dangling SECURITY sentence fixed in 284c8f7, one line; N2 availability nit rolled forward). LANDED main 15792c1 → 284c8f7. 4.7.0 brief: BASEHEAD filled; main CI pending → runner → m5dev single reload (4.6.1 → 4.7.0) with the retire-and-recover drill → this-mac.
- 2026-09-19 RELEASED 4.7.0: release commit 331d004 on main (fast-forward from 284c8f7), tag v4.7.0, pipeline 35459112867 green incl. npm publish, npm latest 4.7.0; PM re-verified main/tag/npm; GitHub release body test count corrected 442→444 (the notes file in git still says 442 — fix with the next docs touch). Cut-over watcher running: m5dev single reload 4.6.1→4.7.0 when quiet + live alias check + retire-and-recover drill; this-mac 4.6.2→4.7.0 when quiet.
- 2026-09-19 CUT OVER: this-mac 4.6.2 → 4.7.0 (watcher, quiet ledger); m5dev 4.6.1 → 4.7.0 with only two queued rows pending (preserved across the reload), brokers healthy. Live alias check on 4.7.0: `career-helper@m5dev` and `engineer@this-mac` → INVALID_ALIAS with the concrete hint (4.6.2 fix confirmed live). Retire-and-recover drill NOT run live: the headless Codex run hit the account's usage limit ("try again at 3:06 PM"); the scenario stands on the e2e fixture + two adversarial reads. Pending: rerun drill-470.sh after the quota resets. emb-138 CLOSED (alias fix live). emb-139 remains open only for the deferred drill.
