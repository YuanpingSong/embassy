---
id: emb-102
title: v3 slice 3 — delete localization; stall notice points at `embassy status`
kind: normal
size: 2
status: draft
release: v3.0.0
updated: 2026-09-02
---

## Binding

**Why**: emb-99 charter, slice 3. zh-CN parity doubled the cost of
every string and doc change for a one-user tool; after emb-100 the
copy-table layer has ~13 live call sites. The zh-CN docs shipped in
the tarball now contradict the English ones (emb-100 F4).

**Deliverable** (base = emb-101 head):
- Delete whole: `locale.ts`, `cli-copy.ts`, `cli-copy.en.ts`,
  `cli-copy.zh-CN.ts`, `README.zh-CN.md`, `docs/*.zh-CN.md`,
  `site/zh-CN/`, `test/public-localization.test.ts` EXCEPT its
  English-doc invariants (the SECURITY/AGENTS/CONTRIBUTING doctrine
  test at ~308–344 and any other pure-English assertion) which
  relocate into `test/public-docs.test.ts`.
- `--lang` option and `DashboardLocale`/`locale` plumbing removed
  through cli.ts, server.ts, providers.ts, claude-peer.ts,
  claude-helper-protocol/-supervisor; English strings inline;
  `callerIdentityConflictHintEn/ZhCn` (exports outside the key table,
  cli.ts ~10–11/751–752) become one inline string.
- WIRE CONTENT: `CLAUDE_PEER_NOTICE_COPY` (claude-peer.ts ~33–48) —
  the stall/diagnostic notices written into live Claude sessions —
  become English-only and say "run `embassy status`" (the dashboard
  they point at no longer exists; docs/DECLINED.md:32–34 recorded the
  dashboard as owner of remedy prose). Tests asserting the zh-CN
  sentences (claude-peer.test.ts ~1782/2288/2290) updated; the
  `<gateway-delivery-stall>`/`<gateway-delivery-diagnostic>` tags and
  attributes stay byte-identical.
- The reset hint "temporarily use Embassy 1.9.x before resetting"
  (cli-copy) → "2.0.x".
- Package manifest / check-npm-package allowlist / npm-package-
  manifest test lose the zh-CN and copy-table entries in the same
  commit. Remove the zh-CN exclusion from test/public-docs.test.ts
  (now the oracle scans everything shipped).
- Docs: README/CONFIGURATION drop the `--lang` mention and the
  zh-CN doc links; site nav loses the zh-CN link.

**Acceptance**: check green; `--help` has no `--lang`; `grep -rin
'zh-CN\|locale\|DashboardLocale\|cliCopy\|getCliCopy' src/ test/
skills/ scripts/ package.json site/ docs/ README.md` zero (justify
residue); a CJK sweep (`grep -rP '[\x{4e00}-\x{9fff}]' src/ test/
docs/ README.md site/ skills/`) zero; the stall-notice test asserts
the new English sentence and unchanged tags; `npm pack --dry-run`
has no zh-CN artifacts.

## Record

- 2026-09-02 Drafted after emb-101 froze.
