---
id: emb-109
title: v3 close — docs to one page, release v3.0.0, cutover both machines, swe3 re-test
kind: normal
size: 3
status: landed
release: v3.0.0
updated: 2026-09-02
---

## Binding

**Why**: emb-99 charter close-out. Founder criteria: daily value,
low maintenance, low friction; "re-test with swe3 once v3 is
actually usable."

**Deliverable** (base = emb-108 head):
- README rewritten as ONE page readable in five minutes: what it is
  (two sentences), install (`npm i -g agent-embassy`; `embassy
  service install`), first message in four commands (register-codex
  inside the task → `embassy status` → `embassy send --from --to` →
  reply), `embassy check`, the shell-peer fallback in one paragraph,
  federation in one paragraph (nodes.json optional), safety in one
  minute (OS boundary, provenance envelope, bounded queues, bodies
  retained locally and shown by `status`), version line naming the
  Claude Code and Codex CLI versions the cutover drill ran against
  (the ONLY place such a claim may live, and only after the drill).
  docs/ reduced to CONFIGURATION, DELIVERY, GATEWAY-ARCHITECTURE
  (trimmed to what exists), SECURITY; DECLINED.md kept as history.
- Housekeeping from the gates: delete or rewrite `.claude/agents/
  content-writer.md` (or add `.claude/` to the oracle roots); scrub
  bare "dashboard" from GATEWAY-ARCHITECTURE and the dead `*copy*.ts`
  glob in AGENTS.md; `site/zh-CN/index.html` redirect stub to the
  English page; `.github/PULL_REQUEST_TEMPLATE.md` live-dashboard
  line; `site/index.html` reduced to a stub paragraph + README link
  (founder: no positioning work); `support/` dir gone; skills
  reinstalled from the release tree on both machines.
- CHANGELOG: `[Unreleased]` → `## [3.0.0] - <date>` with the upgrade
  note (reset-only: move gateway-state.json aside; nodes.json
  optional; re-register Codex tasks; no select/pair); release note
  `.github/release-notes/v3.0.0.md` (the 3.0 story: subtraction,
  what stayed, the four bumps, tested versions).
- Release per `pm/runbooks/release-npm.md` (step 0 CI green on base;
  four version pins; grep-zero for 2.0.1; full check in the landing
  tree; tag v3.0.0; watch pipeline; outside-in verify).
- Cutover both machines: `npm i -g agent-embassy@3.0.0`; stop the
  2.0.1 brokers; move state aside; remove stale dashboard files;
  `embassy service install`; `embassy check`; engineer re-registers;
  live round trip PM ↔ swe3 through `send`; cross-machine proof via
  `await`. Record the Claude Code / Codex CLI versions observed.
- Retire: the `codex-embassy-main` seat's stale registrations; the
  Codex Desktop guidance everywhere.

**Gate**: runbook-runner for the release; PM verifies outside-in.

## Record

- 2026-09-02 Drafted; dispatches after emb-108 lands.
- 2026-09-02 Scope additions from the emb-105 gate (pre-tag):
  CHANGELOG 3.0.0 must not describe a `reply` control result;
  SECURITY.md:178 and DELIVERY.md:79 name `send --conversation`;
  CONTRIBUTING.md:208 / AGENTS.md:113 / .github/ISSUE_TEMPLATE/
  setup_help.yml:25 stop teaching `embassy reply`; code rider:
  provider-neutral CALLER_IDENTITY_REQUIRED for a conversation send
  with no credential; "reply" in the retired-method list with a
  rebuild hint on UNKNOWN_METHOD; byte-level wire equality test;
  direction-inversion test; unconfounded no-credential decoder test;
  peer-alias grammar at the decoder for peer-credentialed sends.
- 2026-09-02 Dispatched to a Fable-tier engineer (Opus limit) on
  worktree v3-109 (branch `v3/emb-109` off main e9a87d9) from the
  pre-written brief incl. the emb-105 doc/code riders and the emb-107
  context update. The release and cutover follow per runbook once it
  lands.
- 2026-09-03 FROZEN by the Fable engineer: head 6f0ef54 on
  `v3/emb-109` (base main e9a87d9), 23 files +447/−738 (src +21/−2,
  cli.ts only); check 509/509 engineer and PM re-runs; marker grep
  zero; README 191 lines with both `status` renders pinned to the
  renderer; every README command grammar-checked live under an empty
  env against a throwaway state dir (the `@your-host` legend reaches
  the host hint; no broker → the no-broker hint); tarball 101 files,
  no zh-CN/dashboard/support/doctor artifacts; CHANGELOG `## [3.0.0]
  - <PM fills the date>` with the upgrade note; release note 26
  lines with the tested-with placeholder; site stub 49 lines, no
  version; content-writer agent rewritten; PR/issue templates and
  AGENTS/CONTRIBUTING/SECURITY brought to v3; oracle roots + terms
  extended; code rider done (CALLER_IDENTITY_REQUIRED + hint; peer
  credential requires `peer-*` --from on both addressing forms;
  UNKNOWN_METHOD rebuild hint; "reply" retired; byte-level wire
  assertion; direction-inversion refusals at the service). Deleted
  site/style.css. Declared: the pinned README renders read `embassy
  2.0.1` and are pinned to EMBASSY_VERSION — the version bump MUST
  sweep them; `--help` omits --succeeds/--expects-reply (pre-existing,
  candidate). Platform-bound tests: none. Adversarial read launched
  (Opus; limit reset).
- 2026-09-03 Adversarial read (Opus): HOLD. F1 bug_report.yml says
  `status` "excludes bodies" while the README says the opposite and
  the pinned renders print bodies (the metadata-only guard scanned
  only README/ARCH); F2 README and release note say the ephemeral
  identity "never reaches disk" while the CHANGELOG in the same
  commit admits the native advertisement record can survive a mid-
  check crash; F3 the new CODEX_THREAD_MISMATCH → "--from must be
  your own alias" hint is a dead-end remedy when NO route exists —
  the exact post-reset state on the documented 2.x→3.0 upgrade path;
  F4 the peer-grammar refusal ships bare; F5 "seventeen" unpinned;
  F6 `<PM fills the date>` is swallowed as inline HTML on GitHub; F7
  UNKNOWN_METHOD hint unreachable from check/status/watch and the
  CHANGELOG sentence inverts who benefits; F8 "verified sender"
  unqualified for the federated hop; F9 `check` ignores the receipt
  response; F10 a never-observed route is eligible and "first" is
  alphabetical; F11 route sends with a codex-* --from and no
  credential still say CLAUDE_IDENTITY_REQUIRED; F12 "tested App
  Server 0.147.0" claims survive in SECURITY/ARCH, DELIVERY "v1",
  zh-CN stub lacks CSP, package.json prose unguarded. Version-sweep
  list delivered: seven check-breaking sites + four stale-but-green
  regex/fixture literals in status-view.test.ts + the unpinned
  "seventeen". Verified sound: all four protocol/schema numbers; the
  upgrade note's code and file name; both pinned renders and the
  hand-reconstructed check render; catalog exclusion; the reply
  hint's token; --expects-reply semantics; register-codex identity;
  the ephemeral request is structurally verified by the closed
  decoder; the mailbox contract; status read-only; nodes.json first-
  boot write; launchd claims; the oracle passes offline; all 21 README
  links resolve; the site CSP tighter; test integrity; the peer-
  prefix rider refuses no legitimate alias.
- PM rulings (13-item correction to the same engineer): bodies stated
  everywhere status is described + guard over all roots; the
  ephemeral claim made exact (advertisement trace named); split the
  missing-route producer with a register hint; peer-grammar hint;
  seventeen pinned to the array; visible date token; UNKNOWN_METHOD
  hint on every path + honest CHANGELOG sentence; attested-sender
  wording; receipt response inspected; eligibility = observed within
  ten minutes, most-recent first; provider-neutral identity code for
  codex-* route sends; tested-0.147.0 claims removed and forbidden;
  DELIVERY v1; zh-CN CSP; package.json prose in the oracle; sweep
  sites derived from EMBASSY_VERSION (seven, not eleven).
- 2026-09-03 Pre-cutover baseline (read-only, both machines, before
  any 3.0.0 install): npm serves agent-embassy 2.0.1; no launchd
  agent loaded on either machine; both 2.0.1 brokers answer health
  (degraded-by-construction on idle connectors). m5dev: Claude Code
  2.1.259, Codex CLI 0.152.0, node v22.23.2, embassy 2.0.1. this-mac:
  Claude Code 2.1.259, Codex CLI 0.152.0, node v22.23.1, embassy
  2.0.1. These are the candidates for the "tested with" line — to be
  CONFIRMED by re-reading them at drill time, never copied from here.
- 2026-09-03 Corrections at f064098 (+313/−113; check 509/509
  engineer and PM re-runs; marker grep zero): bug_report says status
  output may contain bodies and the metadata-only guard scans every
  shipped root (it caught one more ARCH sentence); README/release
  note/CHANGELOG name the native-advertisement trace exactly;
  `assertThread` splits missing-route (ROUTE_UNREGISTERED + a
  register-codex hint) from a true CODEX_THREAD_MISMATCH (its own
  hint); peer-grammar hint; seventeen pinned to the array and derive-
  checked in three docs; visible token `PM-FILLS-DATE`; UNKNOWN_METHOD
  hint on every path and an honest CHANGELOG sentence; attested-
  sender wording; check inspects the receipt response; eligibility =
  observed within ten minutes, most-recent first; provider-neutral
  identity hints for codex-*/peer-* route sends; tested-0.147.0
  claims reworded and word-anchored guards added (the substring form
  tripped on "attested App Server" — correct refusal); DELIVERY v1;
  zh-CN stub CSP; package.json prose scanned; the sweep is SEVEN
  sites (status-view.test.ts derives from EMBASSY_VERSION). PM
  verified F1–F3 directly in the tree.
- 2026-09-03 LANDED: main fast-forwarded e9a87d9 → f064098 (linear).
  CI run 33715629322 watching — the release's step-0 precondition;
  conclusion recorded only when READ. Release runner brief finalized
  (base f064098, expected 509 pass, token PM-FILLS-DATE, seven sweep
  sites).
