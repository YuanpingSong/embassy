---
id: emb-121
title: v3.1 quality 4 — decompress the helper client lifecycle (E6, client-only)
kind: normal
size: 2
status: queued
release: unassigned
updated: 2026-09-03
---

## Binding

claude-helper-supervisor.ts ~46–105: rewrite the compressed multi-statement lines into conventional blocks, one state transition per line, same methods and closures; no preparation object; supervisor extraction NOT included. ~155–200 changed lines, net +35–80 (honest add). FULL protocol gate. Characterize first: a deterministic fake-child transcript for initialize/request correlation, response-vs-timeout, late response, IPC callback error and exit — pending requests settle once; callback/cleanup order; exact IPC objects with fixture request ids; signal order; no extra IPC. Do NOT repair listener lifetimes or homogenize close/forceClose.

**Rules for every quality slice** (from the emb-110 addendum, PM-
ratified): characterization tests are demonstrated green on the
UNCHANGED parent before the refactor, then land with it; no verbs,
flags, safe codes, wire versions, config, persisted fields, output
copy, authority decisions or replay behavior change; no B1–B4 fix
rides a quality slice; output bytes (stdout JSON key order, stderr
lines, exit codes, wire frames, envelopes, persisted state under
fixed clock/ids) stay identical; `TMPDIR=/tmp npm run check` green
and CI green before the next slice.

## Record

- 2026-09-03 Priced from the emb-110 quality addendum (sha cbaafc07…,
  base 8a5e7a1). Order: after emb-112 (A5 barrel first) and emb-120.
