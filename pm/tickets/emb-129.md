---
id: emb-129
title: v3.1 — status-remedy truth follow-ups + dead prewrite codes (from the emb-111 v4 review)
kind: normal
size: 1
status: dispatched
release: unassigned
updated: 2026-09-05
---

## Binding

Five small items surfaced by the emb-111 v4 delta re-check (GO with MINORs), all in status-view / providers / the public-docs guard. Each remedy string must be traced to its producing line in the freeze.

(a) status-view.ts:108–109 — the CLAUDE_PEER_NOT_OBSERVED remedy says "its route retires on the next send if the session exited"; no send path retires a Claude route. The only removals are displacement inside installClaudeRoute when a live same-alias session claims the name (service.ts ~1176–1185), operator removal (~868–879), peer unregister (~1007–1029), ephemeral expiry (~1045–1063). Say what actually happens and the operator move.
(b) status-view.ts:96–97 — the CLAUDE_DISCOVERY_UNAVAILABLE trailing clause "read `embassy status` for the connector's safe code" cannot be satisfied while the line renders: the same busy observation marks the Claude connector healthy and clears its code (service.ts ~2116–2120); after CONNECTOR_OBSERVATION_STALE_AFTER_MS (types.ts:154, 35 s) it shows only the generic CONNECTOR_OBSERVATION_STALE and the route renders stale. Point at what `embassy refresh` itself returns (service.ts ~616).
(c) CLAUDE_PEER_TARGET_STALE / CLAUDE_PEER_TARGET_CHANGED have no throw site in src/ (PM grep at 94573dc: only the set membership providers.ts:58–59 and the status-view remedies; synthesized only by test/gateway-providers.test.ts ~915–917 and test/gateway-service.test.ts ~1881–1883). Delete the two codes, their remedies, and the synthetic fixtures — or name a real producer (none expected).
(d) Pin CLAUDE_BUSY_OBSERVATION_CODES equal to CLAUDE_CLEAN_PREWRITE_RETRY_CODES ∪ {CLAUDE_PEER_NOT_OBSERVED, CLAUDE_DISCOVERY_UNAVAILABLE} so a seventh busy code cannot render silently without a remedy. Pin through an exported constant or rendered output — not a __test facade (emb-112 A4 removes those).
(e) Guard quantifier vocabulary (test/public-docs.test.ts): add only|lone|unique|solitary|just one|exactly one|at most one|no more than one|1 and pin two of the slipping phrasings as inline negative fixtures ("the only registered `codex-*` peer", "a unique `codex-*` peer entry"). Replay over base 8a5e7a1 must still reject the seven base offenders and nothing in the tree.

NOT in scope: CHANGELOG historical entries (history stays history); the availablePeers short-circuit that hides a route remedy for a session last scanned idle (deliberate, pinned at test/status-view.test.ts ~447) — backlog observation only.

Size 1 / E1 ≤50 changed lines (src ≤20, tests ≤25, docs ≤5). Light gate. Proof: guard replay result stated in the freeze; check green; CI read at the exact head.

## Record

- 2026-09-05 Priced from the emb-111 v4 Opus delta re-check (GO with 4 MINORs + 3 NITs; base 94573dc). Dispatched to swe3 ahead of emb-112 while the status-view context is hot.
