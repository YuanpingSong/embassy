---
id: emb-138
title: Alias-shape refusals need their own code and a concrete hint (INVALID_ALIAS)
status: active
opened: 2026-09-19
---
- 2026-09-19 INCIDENT (founder, laptop): a Codex agent reported "registration returned INVALID_GATEWAY_CONFIGURATION; sending to career-orchestrator@this-mac returned ROUTE_UNREGISTERED". PM verification: laptop broker healthy on 4.6.1, config valid, career-orchestrator live (pid 9501, resumed) with a matching ledger row; cross-host send from m5dev delivered (PEER_HANDOFF_CONFIRMED, ack received). Founder found the cause: the agent registered as plain `career-helper` (no `codex-` prefix) → #assertAlias → INVALID_GATEWAY_CONFIGURATION (no CLI hint); the ROUTE_UNREGISTERED most likely came while career-orchestrator was being restarted (its process is a resume). Codex rollouts on m5dev show the same class repeatedly (`engineer@m5dev`, `engineer@this-mac`, the literal doc placeholder `codex-reviewer@your-host`). Dispatched to swe3 (4.6.2): INVALID_ALIAS code + hint naming the real host, docs/skill sentence, tests. Laptop's Codex proxies agents to m5dev and max-ws.lab (no Embassy there) — noted as a source of "wrong host" aliases.
- 2026-09-19 RC 12a9758 (codex/invalid-alias-hint on 90e575e; CI 35456252989 green; check 431/431): INVALID_ALIAS for prefix/bare/wrong-host/placeholder/uppercase/dotted-name refusals with a hint naming the validated local host and the exact shipped grammar (dots host-only); decoder classifies malformed alias strings the same way; config failures unchanged; README EN/CN + SKILL state the rule with pins. PM: diff read, hint text read; live CLI still shows the old code against the running 4.6.1 broker (classification is broker-side) — live check at cut-over. LANDED main 90e575e → 12a9758. 4.6.2 brief/notes drafted.
- 2026-09-19 RELEASED 4.6.2: release commit 15792c1, tag v4.6.2, pipeline 35456670692 green, npm latest 4.6.2 (stale local npm cache masked it until --prefer-online — runbook step 7 updated). Cut-over of both machines queued behind in-flight deliveries (watcher). Live INVALID_ALIAS check runs at the m5dev cut-over.
