---
id: emb-114
title: v3.1 slice 4 — idle health contract (B1)
kind: normal
size: 2
status: queued
release: unassigned
updated: 2026-09-05
---

## Binding

Every connector gets a boot timestamp; the always-local peer mailbox has no observation callbacks, so after 35 s its age alone flips snapshot.health to degraded while the health RPC says ok (verified by the audit with a real service + injected clock). Rule: separate local-mailbox liveness from external observation age; define ONE aggregate contract used by health, snapshot.health and the status renderer (the renderer's own derivation from emb-107 is the reference: shell peers never contribute; an unregistered Codex provider is not evidence of a fault). 10–25 lines. R2 view / R3 service: light gate + fake-clock acceptance across the threshold (active mailbox receipt, no Codex route, a truly failed provider). No new timer, no fabricated observation.

## Record

- 2026-09-03 Priced from the emb-110 audit (artifact sha f1328dae…, base 8a5e7a1). Queued behind slice 3.
- 2026-09-05 Gate v2 (founder-directed bundling): part of unit U3, position 3/5
  (pm/runbooks/gate-v2.md). One commit per ticket id on the unit lane;
  characterization shown green on the ticket's parent commit; landable prefix.
