---
id: emb-100
title: v3 slice 1 — delete both dashboards; re-home discovery refresh
kind: normal
size: 3
status: landed
release: v3.0.0
updated: 2026-09-02
---

## Binding

**Why**: emb-99 charter, slice 1. The static and live dashboards are
~11.2k src lines (6.9k .ts + 4.2k .tsx), ~8k test lines and ~1.9k
asset lines that the founder has never opened; terminal observability
(emb-107) replaces them.

**Deliverable** (base b862513, branch `v3/subtraction`):
- Delete whole: `dashboard-model.ts`, `dashboard.ts`,
  `dashboard-copy.ts/.en.ts/.zh-CN.ts`, `live-dashboard*.ts` (http,
  command, stream, server, protocol, assets, live-dashboard.ts),
  `live-dashboard-app/` (entire dir incl. tsconfig), `assets/
  live-dashboard/`, `assets/vendor/react/`, `site/assets/
  dashboard.png`, `docs/DASHBOARD.md`, `docs/DASHBOARD.zh-CN.md`,
  `docs/THREAT-MODEL-dashboard-mutations.md`, the nine dashboard test
  files + `test/dashboard-fixture.ts` + `test/fixtures/
  dashboard-model/`.
- Re-home: control method `refresh_dashboard` → `refresh_discovery`
  keeping `refreshClaudeDiscovery()` + `recordActivity()`, dropping
  `publish()` and every `publishGatewayDashboard` option/call site;
  CLI `refresh-dashboard` → `refresh` ("Rescan for Claude sessions").
- Delete `remove_codex_registration` (control + service; dashboard-
  only). KEEP `observe_snapshot` (future `watch` backend).
- `server.ts` ready payload loses `DASHBOARD_FILE_NAME`; no
  `gateway-dashboard*.html` is written anywhere.
- Same commit: `package.json` scripts/files/devDeps (`@types/react`
  out; `ws` stays), `tsconfig*.json` excludes, `scripts/check-npm-
  package.mjs` allowlist, `test/npm-package-manifest.test.ts`.
- Docs surgery: README (See it live, refresh-dashboard, dashboard
  rows/mentions), GATEWAY-ARCHITECTURE §Dashboard + §Live companion,
  SECURITY §Live companion boundary, CONTRIBUTING §Local control
  surface, CONFIGURATION live-dashboard port block, AGENTS.md refs,
  `skills/embassy-peer/SKILL.md` (refresh-dashboard block, live-
  dashboard paragraph, frontmatter description). CHANGELOG history
  and past release notes untouched.
- `locale.ts` and `cli-copy*` SURVIVE this slice (emb-102 owns them);
  `claude-peer.ts` notice text untouched (emb-102).

**Acceptance**: `TMPDIR=/tmp npm run check` green in the worktree;
`--help` shows no dashboard command; `npm pack --dry-run` lists no
dashboard/react/zh-CN-dashboard artifacts; `grep -ri dashboard src/`
residue limited to the `DashboardLocale` identifier (renamed in
emb-102) — every other hit listed and justified in the handoff; a
test still exercises `refresh_discovery` as a discovery trigger.

## Record

- 2026-09-02 Dispatched to an Opus engineer subagent (founder ruling:
  Opus subagents build v3; swe3 re-tests once usable).
- 2026-09-02 Engineer handoff: head 78be89b, 80 files, +152/−22,104;
  check 407/407 on engineer run and on PM re-run; CLI surface, pack
  manifest, workflows verified. Engineer findings: (a) the "federated
  named routes are read-only" property now rests on the unregister
  decoder's UUID requirement (→ emb-101 rider); (b) orphaned Codex
  registrations have no operator cleanup path without the dashboard
  (→ emb-107 to consider); (c) SECURITY.md browser-origin doctrine
  bullet left for a deliberate doctrine ruling.
- 2026-09-02 Adversarial read (Opus): HOLD. F1 README moved the
  "metadata-only" claim onto `embassy status`, which emits retained
  bodies, and deleted the disclosure sentence; F2 SECURITY.md
  self-contradiction (doctrine bullet vs "no network listener");
  F3 site bullet advertising port 41961 goes live on merge; F7
  `refresh` swallows discovery failure and journals success; F8 two
  stale architecture lines; F5 no inverse doc oracle. Verified sound:
  the six publish() removals carry no side effect (one false-negative
  path in decide() actually removed), the eight unregister retargets
  are the same store path with settlement intact, shrinkwrap closed
  and drift-free, manifest exact.
- PM rulings: F1, F2, F3, F7, F8 + a negative doc oracle (F5-lite)
  fixed before landing — correction sent to the same engineer.
  Doctrine bullet deletion is ruled as the direct consequence of the
  founder-approved dashboard deletion. Deferred with owners: F4 zh-CN
  contradictions → emb-102 deletes the files; F6 → emb-101 rider
  (assert the property where it is enforced, plus a store/service
  mode refusal); F9 stale gateway-dashboard*.html in state dirs →
  emb-106 boot cleanup + cutover runbook; F10 control protocol version
  → bump at v3.0.0 (emb-109).
- 2026-09-02 Correction commit e3a6b11 (+217/−16: README/architecture
  disclose that `status` carries retained bodies; SECURITY doctrine
  bullet deleted; site listener bullet replaced; `refresh` now returns
  the provider's safe code and journals only completed scans; two
  architecture lines; new test/public-docs.test.ts negative oracle,
  verified to bite). PM read the diff; check 411/411 on PM re-run.
- 2026-09-02 LANDED: main fast-forwarded b862513 → e3a6b11 (linear).
  CI run 33600082875 conclusion: success; site deploy success. Source
  25.7k → 18.7k .ts lines, 0 .tsx; tests 29.2k → 21.8k.
