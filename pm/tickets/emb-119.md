---
id: emb-119
title: v3.1 quality 2 — readable transport failure precedence (E4)
kind: normal
size: 1
status: queued
release: unassigned
updated: 2026-09-03
---

## Binding

control.ts ~1053–1063: the seven-way nested ternary classifying frame failure vs pre-connect errno becomes explicit same-order branches; the writeStarted/ambiguity fence at ~1039–1052 and fail() stay untouched. ~25–40 changed lines, net +5–15. FULL protocol gate. Characterize first: EACCES and unknown errno, a frame-kind × pre/post-write matrix proving timeout/too_large/closed outrank errno and started mutations always become CONTROL_OUTCOME_AMBIGUOUS — driving the real sendGatewayControlRequest with a synthetic socket. Not B2.

**Rules for every quality slice** (from the emb-110 addendum, PM-
ratified): characterization tests are demonstrated green on the
UNCHANGED parent before the refactor, then land with it; no verbs,
flags, safe codes, wire versions, config, persisted fields, output
copy, authority decisions or replay behavior change; no B1–B4 fix
rides a quality slice; output bytes (stdout JSON key order, stderr
lines, exit codes, wire frames, envelopes, persisted state under
fixed clock/ids) stay identical; `TMPDIR=/tmp npm run check` green
and CI green before the next slice.

## Record

- 2026-09-03 Priced from the emb-110 quality addendum (sha cbaafc07…,
  base 8a5e7a1). Order: after emb-118.
