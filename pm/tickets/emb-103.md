---
id: emb-103
title: v3 slice 4 — delete progress watches (`untrack`, TRACK:/DONE:, liveness nudge)
kind: normal
size: 2
status: landed
release: v3.0.0
updated: 2026-09-02
---

## Binding

**Why**: emb-99 charter, slice 4. Progress watches are an opt-in
idle-nudge feature (`TRACK:` prefix / `--track` / `--idle-minutes`,
settled by `DONE:`), separate from busy-gating (verified: the gate is
`providers.ts` ROUTE_BUSY deferral + service requeue). Never used in
this window. ~100 src lines of state machine plus ~330 lines of
service/protocol/envelope plumbing and the automated liveness nudge
that messages an engineer on a timer.

**Deliverable** (base = emb-102 head):
- Delete whole: `progress-watch-machine.ts`, `test/gateway-progress-
  watch-machine.test.ts`; the service watch block (~service.ts
  2311–2515 pre-slice numbering: create/record/inspect/commit/defer,
  the liveness nudge, `settleWatchesForAlias` callers keep working
  as no-ops or are removed with their call sites), `untrack` control
  method + CLI, `TRACK:`/`--track`/`--idle-minutes` parsing,
  `EMBASSY_TRACKING_ENABLED`, `EMBASSY_MAX_WATCHES`, `PublicProgress
  Watch*Snapshot` types + control validators + snapshot fields
  (`progressWatches`, `progressWatchEvents`), the `watch` activity
  kind, `progressWatchActive` through claude-helper-protocol/
  -supervisor and the `<embassy-track-active>` envelope block
  (provenance-envelope.ts ~180–184), `CLI_HINT.progressWatchOwner
  Conflict`, help text rows.
- KEEP `queuedAhead` in the envelope (steering uses it) and
  `EMBASSY_STEERING_ENABLED` / `STEER:` untouched.
- Docs: README `--track` block (~270–274) and mentions; GATEWAY-
  ARCHITECTURE §Progress watches (~684–705) whole; DELIVERY.md
  TRACK/DONE paragraphs; SKILL.md TRACK/DONE/untrack teaching;
  relocated public-docs assertions that pin TRACK/DONE/untrack
  wording are DELETED (they guard a removed feature); add `--track`,
  `TRACK:`, `untrack`, `idle-minutes`, `progress watch` to the
  forbidden-term oracle. CHANGELOG Unreleased: Removed line.
- Snapshot shape: dropping `progressWatches`/`progressWatchEvents`
  is a control-result shape change — control protocol is already 3
  for the v3 line; no further bump.

**Acceptance**: check green; `--help` has no `untrack`/`--track`;
grep for `track|watch` residue in src/ limited to justified hits
(`observe_snapshot`, `refreshPeers`' wake loop, `EMBASSY_STEERING`);
`embassy status` JSON has no `progressWatches` key (test); a
`TRACK:`-prefixed body is delivered verbatim as an ordinary message
(test) — no silent stripping; workflows untouched.

## Record

- 2026-09-02 Drafted after emb-102 froze.
- 2026-09-02 Dispatched to a Fable-tier engineer on base b219bb1 =
  main; brief requires a grep proving the ROUTE_BUSY deferral path
  is unchanged and verbatim-delivery tests for TRACK:/DONE: bodies.
- 2026-09-02 FROZEN by the Fable engineer: head 7a7f3ce, 25 files
  +116/−1,177 (src +42/−563; test +65/−569); check 355/355 engineer
  and PM re-runs; `--help` clean; providers.ts ROUTE_BUSY/activeTurns
  diff-count 0 (busy gate untouched). Watches were never persisted;
  control validator now rejects the stale snapshot keys; helper IPC
  rejects `progressWatchActive`; `<embassy-track-active>` left the
  reserved-tag pattern. Surprise: `watch_owner_conflict` was
  unreachable in production (decisionFor mapped it to `conflict`).
  Adversarial read launched (Fable).
- 2026-09-02 Adversarial read (Fable): GO. Three LOW → riders on
  emb-104: the removed-flag refusal test is vacuous for send/reply;
  send lost its only rejected-decision coverage at wire and CLI; two
  docs enumerate two neutralized tags where the pattern has three.
  Verified sound: every settleWatchesForAlias call site lost only
  that statement; wake loop keeps expiry/prune/discovery/peer refresh;
  reserved tags exact per-tag tested; no DONE:/TRACK: instruction
  survives; `--expects-reply` is a real feature; doc counts match
  (19 methods, 18 commands).
- 2026-09-02 LANDED: main fast-forwarded b219bb1 → 7a7f3ce (linear);
  CI run 33649721824 conclusion: success.
