---
id: emb-130
title: peer-mailbox receipt tests flake on Ubuntu — 100 ms receipt timeout vs runner speed; expired receipt reported as route_mismatch
kind: bug
size: 1
status: landed
release: unassigned
updated: 2026-09-05
---

## Binding

Evidence (PM-read): CI run 33989439531 attempt 1 (emb-129 lane, head 320422a): Ubuntu 22 failed test 286 "a peer waiter kicks cleanly deferred mail and exact receipt settles it once" (gateway-service.test.ts:1068, duration 232 ms); Ubuntu 20 failed test 287 "native Claude STEER text reaches a peer mailbox only through the ordinary lane" (:1100). Both expected peerReceipt `{accepted:true, code:"ok"}` and got `{accepted:false, code:"route_mismatch"}`. Attempt 2 (unchanged head) green. Test 288 "queued peer mail resumes once after restart…" (same family) was already in the backlog as intermittent. The emb-129 diff in that file is two deleted lines in an unrelated synthetic code set; the failing tests are untouched. Saved log: job tmp emb129-ci/attempt1-failed.log sha256 5b64760f22a3cd22d9046e3f4e5d69fe32d050c8c082e3054a163a7617273299.

Mechanism (PM-traced, to be confirmed by characterization): both fixtures build LocalPeerMailboxProvider with `receiptTimeoutMs: 100` and a frozen `now`. peer-mailbox.ts:96–97 arms a real setTimeout for min(receiptTimeoutMs, deadline−now) that deletes the receipt and resolves false; if the runner takes >100 ms between awaitPeer resolving and the peerReceipt call, the receipt is gone; peer-mailbox.ts:66–74 RETURNS "rejected" (it does not throw), service.ts:1092 manufactures ROUTE_UNREGISTERED from that rejection, and decisionFor at :268–270 buckets it as `route_mismatch` for a peer principal. (Engineer correction accepted verbatim, 2026-09-05.)

Scope: (1) characterization first — a deterministic test that lets the receipt timer fire before the receipt call (fake timers or receiptTimeoutMs: 0) and asserts the exact code today; name the BridgeError code the mailbox throws for an expired/unknown receipt (file:line). (2) Fixture honesty — the three tests' 100 ms timeout is a runner-speed assumption; either fake the timer or raise it to a bound that cannot be crossed by a slow runner, without slowing the suite (state the new bound and why). (3) Code honesty — if an expired receipt really maps to `route_mismatch`, decide whether that is the truthful public code (it tells an operator the route changed when the receipt merely expired); if not, map it to the existing `not_found`/`expired`-class code and document it (control.ts:126/450 unions). No new concept; no new public code unless one of the existing ones is truthful.

Size 1 / E1 ≤50 changed lines (src ≤15, tests ≤35). Light gate. Proof: the characterization fails on the parent and passes after; the three tests survive 20 local iterations (`node --test --test-repeat` or a loop); CI READ at the exact head.

## Record

- 2026-09-05 Filed from the emb-129 CI attempt-1 failure (PM read the failed-job logs; both fixtures traced to peer-mailbox.ts:96–97). Queued right after emb-129, BEFORE emb-112: a false red on every third landing is operational friction.
- 2026-09-05 Dispatched to swe3 right after emb-129 landed; base main 320422a;
  branch v3.1/emb-130. emb-112 waits behind it.
- 2026-09-05 Engineer's evidence supplement (crossed with the GO): attempt-1
  raw job logs preserved on m5dev — /tmp/emb129-ci-attempt1-ubuntu22.log
  sha 7fbacfd7… (job 101368910148) and …-ubuntu20.log sha df70f4a7…
  (job 101368910167); failed attempt URL …/runs/33989439531/attempts/1.
  Refined mechanism (engineer, hypothesis not proof): service.ts:1091–
  1092 maps an adapter receipt rejection to ROUTE_UNREGISTERED →
  decisionFor :268–270 → route_mismatch for a peer principal; peer-
  mailbox.ts:70–71 rejects when the receipt is absent or same() at :118
  (alias, routeHandle, registrationId) fails; the timer at :95–98 deletes
  the pending entry after 100 ms wall-clock — BEFORE the waiter result
  (:100) and the async acceptance/persistence (:101) necessarily finish,
  so store I/O on a slow runner is inside the window, not only the
  awaitPeer→peerReceipt gap. Binding item (1) must characterize that
  path (timer fires during acceptance/persistence) as well. Shared-
  fixture answer: NO — the :1881 edit is a local array in a later test.
- 2026-09-05 Linear mirror: emb-130 → 2185LAB-356 (In Progress, estimate 1,
  Bug). emb-129 → 2185LAB-355 Done.
- 2026-09-05 Engineer premise correction (accepted verbatim): "peer-mailbox.ts:
  66–74 returns 'rejected', it does not throw for expired/unknown receipts.
  service.ts:1092 manufactures ROUTE_UNREGISTERED, then :268–270 buckets it
  as route_mismatch." Characterization will hold onAccepted while the timer
  expires. SCOPE SPLIT: the backlog restart test's recorded failure is at
  test:1133 (re-registration), BEFORE any receipt timer exists — a
  different mechanism, NOT explained by this timeout; it stays a separate
  backlog item (emb-110 backlog) and does not ride this slice. Only that
  test's later receipt assertion shares the 100 ms assumption and is
  covered by item (2).
- 2026-09-05 FROZEN at d5be488 (base 320422a). PM light gate: head/base/patch
  SHA match; clean; check 512/512; CI run 33990354721 first attempt READ —
  all five legs SUCCESS; accounting from the diff = src 9/15, tests 33/35,
  docs 4/5, total 46/50; diff --check clean, 0 markers, 0 added TODO. PM
  read the 46-line diff: expired/unknown receipt → existing `not_found`
  (decide() now passes an explicit decision through without a revision
  bump); no consumer in src distinguishes route_mismatch for receipts
  (cli.ts:680 keys not_found on `send` only) and no doc mentions it; the
  three fixtures 100 → 10 000 ms = their message deadline, timers
  cancelled on ack/close; 60/60 repeat. Evidence ruling: the
  characterization crosses the acceptance-in-flight window at the
  callback boundary (holds onAccepted before the original callback
  runs); a literally suspended store write is NOT required — same code
  path, same refusal. Parent characterization green (sha 824bd3ac…) and
  parent regression red (sha 2bd209d7…) as the rule requires.
- 2026-09-05 LANDED: main fast-forwarded 320422a → d5be488. CI on main:
  pending READ.
